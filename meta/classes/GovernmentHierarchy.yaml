id: https://nde.nl/ontology/hc/class/GovernmentHierarchy
name: GovernmentHierarchy
title: Government Hierarchy Class
prefixes:
  linkml: https://w3id.org/linkml/
  hc: https://nde.nl/ontology/hc/
  org: http://www.w3.org/ns/org#
default_prefix: hc
imports:
  - linkml:types
  - ../slots/has_label
  - ../slots/has_tier
classes:
  GovernmentHierarchy:
    class_uri: org:OrganizationalUnit
    description: >-
      Administrative tier structure representing governmental organizational levels and jurisdictional relationships.
    alt_descriptions:
      nl: >-
        Administratieve laagstructuur die gouvernementele organisatieniveaus en jurisdictionele relaties vertegenwoordigt.
      de: >-
        Verwaltungsebenenstruktur, die staatliche Organisationsebenen und Jurisdiktionsbeziehungen darstellt.
      fr: >-
        Structure de niveau administratif représentant les niveaux organisationnels gouvernementaux et les relations de juridiction.
      es: >-
        Estructura de nivel administrativo que representa niveles organizacionales gubernamentales y relaciones jurisdiccionales.
      ar: >-
        هيكل المستوى الإداري الذي يمثل مستويات التنظيم الحكومي والعلاقات القضائية.
      id: >-
        Struktur tingkat administratif yang mewakili tingkat organisasi pemerintahan dan hubungan yurisdiksi.
      zh: >-
        代表政府组织层级和管辖关系的行政层阶结构。
    structured_aliases:

      - literal_form: overheidshiërarchie

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: bestuurlijke structuur

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Regierungshierarchie

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Verwaltungsstruktur

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: hiérarchie gouvernementale

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: structure administrative

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: jerarquía gubernamental

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: estructura administrativa

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: التسلسل الحكومي

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: الهيكل الإداري

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: hierarki pemerintahan

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: struktur administratif

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 政府层级

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: 行政架构

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: government hierarchy

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: administrative tier

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: jurisdictional level

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: public administration

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: governmental structure

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: org:OrganizationalUnit

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_label

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_tier

        predicate: EXACT_SYNONYM

        in_language: zh
