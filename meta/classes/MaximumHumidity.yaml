id: https://nde.nl/ontology/hc/class/MaximumHumidity
name: MaximumHumidity
title: Maximum Humidity Class
prefixes:
  linkml: https://w3id.org/linkml/
  hc: https://nde.nl/ontology/hc/
  schema: http://schema.org/
default_prefix: hc
imports:
- linkml:types
- ../slots/has_value
- ../slots/has_measurement_unit
classes:
  MaximumHumidity:
    class_uri: schema:QuantitativeValue
    description: >-
      Upper threshold for atmospheric moisture content expressed as a percentage, applied in preservation environments.
    alt_descriptions:
      nl: >-
        Bovengrens voor atmosferisch vochtgehalte uitgedrukt als percentage, toegepast in bewaaromgevingen.
      de: >-
        Obergrenze für atmosphärische Feuchtigkeit als Prozentsatz, angewendet in Konservierungsumgebungen.
      fr: >-
        Seuil supérieur de teneur en humidité atmosphérique exprimé en pourcentage, appliqué dans les environnements de conservation.
      es: >-
        Límite superior de contenido de humedad atmosférica expresado como porcentaje, aplicado en entornos de preservación.
      ar: >-
        الحد الأعلى لمحتوى الرطوبة الجوية معبراً عنه كنسبة مئوية، مطبق في بيئات الحفظ.
      id: >-
        Batas atas kandungan kelembaban atmosfer dinyatakan sebagai persentase, diterapkan di lingkungan pelestarian.
      zh: >-
        以百分比表示的大气湿度含量上限，应用于保存环境。
    structured_aliases:

      - literal_form: maximale luchtvochtigheid

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: vochtigheidsbovengrens

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: maximale Luftfeuchtigkeit

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Feuchtigkeitsobergrenze

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: humidité maximale

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: seuil d'humidité

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: humedad máxima

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: límite de humedad

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: الرطوبة القصوى

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: حد الرطوبة الأعلى

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: kelembaban maksimum

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: batas kelembaban atas

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 最大湿度

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: 湿度上限

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: humidity

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: environmental control

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: preservation

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: threshold

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: conservation

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: schema:QuantitativeValue

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_value

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_measurement_unit

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: 'Preserved from prior description: Upper relative-humidity boundary value used in environmental constraints.'

        predicate: EXACT_SYNONYM

        in_language: zh
