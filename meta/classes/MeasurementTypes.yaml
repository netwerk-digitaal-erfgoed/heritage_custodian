id: https://nde.nl/ontology/hc/class/MeasurementTypes
name: MeasurementTypes
title: Measurement Types Class Module
prefixes:
  linkml: https://w3id.org/linkml/
  hc: https://nde.nl/ontology/hc/
  qudt: http://qudt.org/schema/qudt/
  crm: http://www.cidoc-crm.org/cidoc-crm/
  schema: http://schema.org/
default_prefix: hc
imports:
  - ./MeasurementType
  - linkml:types
  - ../slots/has_name
classes:
  TemperatureMeasurement:
    is_a: MeasurementType
    class_uri: hc:TemperatureMeasurement
    description: >-
      Quantitative assessment of thermal energy level within an environmental monitoring context.
    alt_descriptions:
      nl: >-
        Kwantitatieve beoordeling van het thermische energieniveau binnen een milieumonitoringcontext.
      de: >-
        Quantitative Bewertung des thermischen Energieniveaus in einem Umweltüberwachungskontext.
      fr: >-
        Évaluation quantitative du niveau d'énergie thermique dans un contexte de surveillance environnementale.
      es: >-
        Evaluación cuantitativa del nivel de energía térmica en un contexto de monitoreo ambiental.
      ar: >-
        تقييم كمي لمستوى الطاقة الحرارية في سياق المراقبة البيئية.
      id: >-
        Penilaian kuantitatif tingkat energi termal dalam konteks pemantauan lingkungan.
      zh: >-
        环境监测背景下热能水平的定量评估。
    structured_aliases:

      - literal_form: TemperatuurMeting

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Temperaturmessung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: MesureTempérature

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: MediciónTemperatura

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: قياس الحرارة

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: PengukuranSuhu

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 温度测量

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: LuchtvochtigheidMeting

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Luftfeuchtigkeitsmessung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: MesureHumidité

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: MediciónHumedad

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: قياس الرطوبة

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: PengukuranKelembaban

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 湿度测量

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: LichtniveauMeting

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Lichtstärkemessung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: MesureÉclairement

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: MediciónNivelLuz

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: قياس مستوى الضوء

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: PengukuranTingkatCahaya

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 光照度测量

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: UVStralingMeting

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: UV-Strahlungsmessung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: MesureRayonnementUV

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: MediciónRadiaciónUV

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: قياس الأشعة فوق البنفسجية

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: PengukuranRadiasiUV

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 紫外线辐射测量

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: LuchtkwaliteitMeting

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Luftqualitätsmessung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: MesureQualitéAir

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: MediciónCalidadAire

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: قياس جودة الهواء

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: PengukuranKualitasUdara

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 空气质量测量

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: CO2Meting

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: CO2-Messung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: MesureCO2

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: MediciónCO2

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: قياس ثاني أكسيد الكربون

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: PengukuranCO2

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 二氧化碳测量

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: VOCMeting

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: VOC-Messung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: MesureCOV

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: MediciónCOV

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: قياس المركبات العضوية المتطايرة

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: PengukuranVOC

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 挥发性有机物测量

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: TrillingMeting

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Vibrationsmessung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: MesureVibration

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: MediciónVibración

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: قياس الاهتزاز

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: PengukuranVibrasi

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 振动测量

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: PlaagtellingMeting

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Schädlingszählungsmessung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: MesureComptageNuisibles

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: MediciónConteoPlagas

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: قياس عدد الآفات

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: PengukuranJumlahHama

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 害虫计数测量

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: WateractiviteitMeting

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Wasseraktivitätsmessung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: MesureActivitéEau

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: MediciónActividadAgua

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: قياس نشاط الماء

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: PengukuranAktivitasAir

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 水活度测量

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: >-

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: DauwpuntMeting

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Taupunktmessung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: MesurePointRosée

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: MediciónPuntoRocío

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: قياس نقطة الندى

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: PengukuranTitikEmbun

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 露点测量

        predicate: EXACT_SYNONYM

        in_language: zh
