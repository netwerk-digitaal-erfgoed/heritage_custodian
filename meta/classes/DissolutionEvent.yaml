id: https://nde.nl/ontology/hc/class/DissolutionEvent
name: DissolutionEvent
title: Dissolution Event Class
description: Event of organization dissolution or end of legal status. MIGRATED from dissolution_date and dissolved_date (2026-01-26).
prefixes:
  hc: https://nde.nl/ontology/hc/
  prov: http://www.w3.org/ns/prov#
  org: http://www.w3.org/ns/org#
imports:
  - linkml:types
  - ../slots/temporal_extent
default_prefix: hc
classes:
  DissolutionEvent:
    class_uri: org:ChangeEvent
    description: >-
      Formal event marking the termination of an organization and cessation of its legal existence.
    alt_descriptions:
      nl: >-
        Formeel evenement dat de beëindiging van een organisatie en het einde van haar juridische bestaan markeert.
      de: >-
        Formelles Ereignis, das die Auflösung einer Organisation und die Beendigung ihrer rechtlichen Existenz markiert.
      fr: >-
        Événement formel marquant la dissolution d'une organisation et la cessation de son existence légale.
      es: >-
        Evento formal que marca la disolución de una organización y el cese de su existencia legal.
      ar: >-
        حدث رسمي يصنف حل المنظمة وإنهاء وجودها القانوني.
      id: >-
        Peristiwa formal yang menandai pembubaran organisasi dan penghentian keberadaan hukumnya.
      zh: >-
        标志组织终止及其法律存在终结的正式事件。
    structured_aliases:

      - literal_form: ontbindingsevenement

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: beëindigingsgebeurtenis

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Auflösungsereignis

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Beendigungsereignis

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: événement de dissolution

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: événement de clôture

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: evento de disolución

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: evento de cierre

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: حدث الحل

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: حدث الإغلاق

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: peristiwa pembubaran

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: peristiwa penutupan

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 解散事件

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: 终止事件

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: dissolution

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: termination

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: closure

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: organization end

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: legal cessation

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: org:ChangeEvent

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: prov:Activity

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: temporal_extent

        predicate: EXACT_SYNONYM

        in_language: zh
