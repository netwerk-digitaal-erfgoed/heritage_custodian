id: https://nde.nl/ontology/hc/class/DiarizationStatus
name: diarization_status_class
title: DiarizationStatus Class
prefixes:
  linkml: https://w3id.org/linkml/
  hc: https://nde.nl/ontology/hc/
  schema: http://schema.org/
default_prefix: hc
imports:
  - linkml:types
  - ../slots/identified_by
  - ../slots/has_label
classes:
  DiarizationStatus:
    class_uri: schema:ActionStatusType
    description: >-
      Processing-state indicator for speaker segmentation and identification workflows in audio analysis.
    broad_mappings:
      - schema:ActionStatusType
    alt_descriptions:
      nl: >-
        Verwerkingsstatus-indicator voor sprekersegmentatie- en identificatieworkflows in audioanalyse.
      de: >-
        Verarbeitungsstatusindikator für Sprechersegmentierungs- und Identifizierungsworkflows in der Audioanalyse.
      fr: >-
        Indicateur d'état de traitement pour les workflows de segmentation et d'identification des locuteurs dans l'analyse audio.
      es: >-
        Indicador de estado de procesamiento para flujos de trabajo de segmentación e identificación de hablantes en análisis de audio.
      ar: >-
        مؤشر حالة المعالجة لعمليات تجزئة المتحدث وتحديد الهوية في تحليل الصوت.
      id: >-
        Indikator status pemrosesan untuk alur kerja segmentasi dan identifikasi pembicara dalam analisis audio.
      zh: >-
        音频分析中说话人分割和识别工作流的处理状态指示器。
    structured_aliases:

      - literal_form: diarisatiestatus

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: sprekerscheidingsstatus

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Diarisierungsstatus

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Sprechertrennungsstatus

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: statut de diarisation

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: état de segmentation

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: estado de diarización

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: estado de segmentación

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: حالة التفريق

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: حالة تجزئة المتحدث

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: status diarisasi

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: status segmentasi pembicara

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 说话人分离状态

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: 语音分割状态

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: diarization status

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: speaker segmentation

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: audio processing

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: speaker identification

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: processing state

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: identified_by

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_label

        predicate: EXACT_SYNONYM

        in_language: zh
