id: https://nde.nl/ontology/hc/class/OrganizerRole
name: OrganizerRole
title: Organizer Role Class

prefixes:
  linkml: https://w3id.org/linkml/
  hc: https://nde.nl/ontology/hc/
  schema: http://schema.org/
  skos: http://www.w3.org/2004/02/skos/core#
  prov: http://www.w3.org/ns/prov#

default_prefix: hc

imports:
  - linkml:types
  - ../metadata
classes:
  OrganizerRole:
    class_uri: schema:Role
    description: >-
      Functional capacity of an entity in coordinating, planning, or producing an event, exhibition, or program.
    alt_descriptions:
      nl: >-
        Functionele rol van een entiteit bij het coördineren, plannen of produceren van een evenement, tentoonstelling of programma.
      de: >-
        Funktionale Kapazität einer Entität bei der Koordination, Planung oder Produktion einer Veranstaltung, Ausstellung oder eines Programms.
      fr: >-
        Capacité fonctionnelle d'une entité à coordonner, planifier ou produire un événement, une exposition ou un programme.
      es: >-
        Capacidad funcional de una entidad para coordinar, planificar o producir un evento, exposición o programa.
      ar: >-
        القدرة الوظيفية لكيان في التنسيق أو التخطيط أو إنتاج حدث أو معرض أو برنامج.
      id: >-
        Kapasitas fungsional suatu entitas dalam mengkoordinasikan, merencanakan, atau memproduksi acara, pameran, atau program.
      zh: >-
        实体在协调、策划或制作活动、展览或项目中的功能能力。
    structured_aliases:

      - literal_form: organisatorrol

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: coördinatiefunctie

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Veranstalterrolle

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Koordinationsfunktion

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: rôle d'organisateur

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: fonction de coordination

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: rol de organizador

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: función de coordinación

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: دور المنظم

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: وظيفة التنسيق

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: peran penyelenggara

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: fungsi koordinasi

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 组织者角色

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: 协调职能

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: organizer role

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: event coordination

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: exhibition organizer

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: program planner

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: production role

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: schema:Role

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: prov:Role

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: role_type

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: role_description

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: "value: PRIMARY"

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: "value: CO_ORGANIZER"

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: |

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: "PRIMARY: Main organizing institution"

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: "CO_ORGANIZER: Partner institution with significant organizational role"

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: "SPONSOR_ORGANIZER: Sponsor with curatorial/organizational input"

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: "LENDING_INSTITUTION: Institution lending objects with exhibition involvement"

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: "HOST_VENUE: Venue hosting a traveling exhibition"

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: "**Primary**: `schema:Role` - Schema.org role"

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: "**Close**: `prov:Role` - PROV-O role in activity"

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: "Created per slot_fixes.yaml revision for co_organized_by migration"

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: "Used with Organizer class to specify organizational involvement"

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: "RULE 53: Part of co_organized_by → has_organizer + Organizer migration"

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: "OrganizerRoleTypeEnum provides controlled values (PRIMARY, CO_ORGANIZER, SPONSOR_ORGANIZER, LENDING_INSTITUTION, HOST_VENUE, SUPPORTING_PARTNER, ADVISOR)."

        predicate: EXACT_SYNONYM

        in_language: zh
