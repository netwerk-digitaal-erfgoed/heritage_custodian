id: https://nde.nl/ontology/hc/classes/TimeEntryType
name: TimeEntryType
title: TimeEntryType
prefixes:
  linkml: https://w3id.org/linkml/
  hc: https://nde.nl/ontology/hc/
  schema: http://schema.org/
  prov: http://www.w3.org/ns/prov#
  xsd: http://www.w3.org/2001/XMLSchema#
  skos: http://www.w3.org/2004/02/skos/core#
  time: http://www.w3.org/2006/time#
imports:
  - linkml:types
# default_range: string
classes:
  TimeEntryType:
    alt_descriptions:
      nl: Typeclassificatie voor een tijdinvoer die de semantische rol van een tijdspunt aangeeft.
      de: Typklassifikation fuer einen Zeiteintrag, die die semantische Rolle eines Zeitpunkts angibt.
      fr: Classification de type pour une entree temporelle indiquant le role semantique d un point temporel.
      es: Clasificacion de tipo para una entrada temporal que indica el rol semantico del punto temporal.
      ar: تصنيف نوع لمدخل زمني يحدد الدور الدلالي للنقطة الزمنية.
      id: Klasifikasi tipe untuk entri waktu yang menyatakan peran semantik titik temporal.
      zh: 用于标注时间点语义角色的时间条目类型分类。
    structured_aliases:
    - literal_form: type tijdinvoer
      in_language: nl
    - literal_form: Typ Zeiteintrag
      in_language: de
    - literal_form: type d entree temporelle
      in_language: fr
    - literal_form: tipo de entrada temporal
      in_language: es
    - literal_form: نوع إدخال زمني
      in_language: ar
    - literal_form: tipe entri waktu
      in_language: id
    - literal_form: 时间条目类型
      in_language: zh
    description: >-
      Category indicating the semantic role of a temporal point marker (e.g., founded, dissolved, started, ended).
    class_uri: skos:Concept
    close_mappings:
    - time:TemporalEntity
    broad_mappings:
    - skos:Concept
    annotations:
      specificity_score: 0.1
      specificity_rationale: Generic utility class/slot created during migration
      custodian_types: '[''*'']'
      modeling_notes: |
        Mapping rationale

        - class_uri: skos:Concept for controlled vocabulary semantics
        - close_mappings: time:TemporalEntity
        - related_mappings: schema:PropertyValue
    slots: []
