id: https://nde.nl/ontology/hc/classes/ConversionRateType
name: ConversionRateType
title: ConversionRateType
prefixes:
  linkml: https://w3id.org/linkml/
  hc: https://nde.nl/ontology/hc/
  skos: http://www.w3.org/2004/02/skos/core#
default_prefix: hc
imports:
  - linkml:types
  - ../slots/has_denominator_data
  - ../slots/has_numerator
  - ../slots/has_label
  - ../slots/has_benchmark
classes:
  ConversionRateType:
    abstract: true
    class_uri: skos:Concept
    description: >-
      Abstract classification for metrics measuring the proportion of one population that completes a target action.
    alt_descriptions:
      nl: >-
        Abstracte classificatie voor statistieken die de verhouding meten van een populatie die een doelactie voltooit.
      de: >-
        Abstrakte Klassifikation für Metriken, die den Anteil einer Population messen, der eine Zielaktion abschließt.
      fr: >-
        Classification abstraite pour les métriques mesurant la proportion d'une population qui effectue une action cible.
      es: >-
        Clasificación abstracta para métricas que miden la proporción de una población que completa una acción objetivo.
      ar: >-
        تصنيف تجريدي للمقاييس التي تقيس نسبة السكان الذين يكملون إجراءً مستهدفًا.
      id: >-
        Klasifikasi abstrak untuk metrik yang mengukur proporsi populasi yang menyelesaikan tindakan target.
      zh: >-
        衡量完成目标动作的人群比例的度量抽象分类。
    structured_aliases:

      - literal_form: conversieratetype

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: omzettingspercentage

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Konversionsratentyp

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Umwandlungsrate

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: type de taux de conversion

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: taux de transformation

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: tipo de tasa de conversión

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: porcentaje de conversión

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: نوع معدل التحويل

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: نسبة التحويل

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: jenis tingkat konversi

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: rasio konversi

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 转化率类型

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: 转化率

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: conversion rate

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: performance metric

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: engagement

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: audience analytics

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: KPI

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: skos:Concept

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_label

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_denominator_data

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_numerator

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_benchmark

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: |

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: **Visitor to Purchase**: Museum visitors who buy from gift shop

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: **Visitor to Member**: Visitors who become members

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: **Online to Visit**: Website visitors who visit physically

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: **Email to Click**: Email recipients who click links

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: **Social to Follower**: Social media viewers who follow

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: This file: Abstract `ConversionRateType` base class

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: ConversionRateTypes.yaml: Concrete subclasses

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: class_uri: skos:Concept (type taxonomy concept)

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Part of SKOS-based classification scheme

        predicate: EXACT_SYNONYM

        in_language: zh
