id: https://nde.nl/ontology/hc/class/DataFormatTypes
name: data_format_types
title: Data Format Types
prefixes:
  linkml: https://w3id.org/linkml/
  hc: https://nde.nl/ontology/hc/
  skos: http://www.w3.org/2004/02/skos/core#
default_prefix: hc
imports:
  - ./DataFormat
  - linkml:types
classes:
  JsonFormat:
    is_a: DataFormatType
    class_uri: hc:JsonFormat
    description: >-
      Lightweight data interchange format using human-readable text for structured data serialization.
    alt_descriptions:
      nl: Lichtgewicht gegevensuitwisselingsformaat dat menselijk leesbare tekst gebruikt voor gestructureerde gegevensserialisatie.
      de: Leichtes Datenaustauschformat, das menschenlesbaren Text für strukturierte Datenserialisierung verwendet.
      fr: Format d'échange de données léger utilisant du texte lisible par l'homme pour la sérialisation de données structurées.
      es: Formato de intercambio de datos ligero que utiliza texto legible por humanos para la serialización de datos estructurados.
      ar: تنسيق تبادل بيانات خفيف الوزن يستخدم نصًا مقروءًا بشريًا لتسلسل البيانات المنظمة.
      id: Format pertukaran data ringan yang menggunakan teks yang dapat dibaca manusia untuk serialisasi data terstruktur.
      zh: 使用人类可读文本进行结构化数据序列化的轻量级数据交换格式。
    structured_aliases:

      - literal_form: JsonFormaat

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: JsonFormat

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: FormatJson

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: FormatoJson

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: تنسيق جسون

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: FormatJson

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: JSON格式

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Opmaaktaalformaat voor het coderen van gestructureerde documenten met aanpasbare tags.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Auszeichnungssprachenformat zur Kodierung strukturierter Dokumente mit anpassbaren Tags.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Format de langage de balisage pour encoder des documents structurés avec des balises personnalisables.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Formato de lenguaje de marcado para codificar documentos estructurados con etiquetas personalizables.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: تنسيق لغة ترميز لترميز المستندات المنظمة باستخدام علامات قابلة للتخصيص.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Format bahasa markup untuk mengkodekan dokumen terstruktur menggunakan tag yang dapat disesuaikan.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 使用可自定义标签编码结构化文档的标记语言格式。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: XmlFormaat

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: XmlFormat

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: FormatXml

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: FormatoXml

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: تنسيق إكس إم إل

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: FormatXml

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: XML格式

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Tabelgegevensformaat dat komma-scheidingstekens gebruikt om veldwaarden in platte tekst te scheiden.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Tabellendatenformat, das Komma-Trennzeichen verwendet, um Feldwerte in Klartext zu trennen.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Format de données tabulaires utilisant des virgules comme délimiteurs pour séparer les valeurs de champs en texte brut.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Formato de datos tabulares que usa delimitadores de coma para separar valores de campos en texto plano.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: تنسيق بيانات جدولي يستخدم فواصل فاصلة لفصل قيم الحقول في نص عادي.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Format data tabular yang menggunakan pembatas koma untuk memisahkan nilai bidang dalam teks biasa.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 使用逗号分隔符在纯文本中分隔字段值的表格数据格式。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: CsvFormaat

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: CsvFormat

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: FormatCsv

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: FormatoCsv

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: تنسيق سي إس في

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: FormatCsv

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: CSV格式

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: W3C-standaard serialisatieformaat voor RDF-grafen met XML-syntaxis.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: W3C-Standard-Serialisierungsformat für RDF-Graphen unter Verwendung der XML-Syntax.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Format de sérialisation standard W3C pour les graphes RDF utilisant la syntaxe XML.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Formato de serialización estándar W3C para grafos RDF usando sintaxis XML.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: تنسيق تسلسل قياسي W3C لرسوم RDF باستخدام بناء جملة XML.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Format serialisasi standar W3C untuk graf RDF menggunakan sintaks XML.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 使用XML语法的RDF图W3C标准序列化格式。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: RdfXmlFormaat

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: RdfXmlFormat

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: FormatRdfXml

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: FormatoRdfXml

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: تنسيق RDF/XML

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: FormatRdfXml

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: RDF/XML格式

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Compact menselijk leesbare RDF-serialisatie met afgekorte triplesyntaxis.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Kompakte menschenlesbare RDF-Serialisierung mit abgekürzter Triple-Syntax.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Sérialisation RDF compacte et lisible par l'homme utilisant une syntaxe de triple abrégée.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Serialización RDF compacta y legible por humanos usando sintaxis de triples abreviada.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: تسلسل RDF مضغوط ومقروء بشريًا باستخدام بناء جملة ثلاثي مختصر.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Serialisasi RDF yang ringkas dan dapat dibaca manusia menggunakan sintaks tigaan yang disingkat.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 使用缩写三元语法的紧凑人类可读RDF序列化格式。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: TurtleFormaat

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: TurtleFormat

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: FormatTurtle

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: FormatoTurtle

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: تنسيق تيرتل

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: FormatTurtle

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: Turtle格式

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Op JSON gebaseerd serialisatieformaat voor het uitdrukken van gekoppelde gegevens met context.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: JSON-basiertes Serialisierungsformat zum Ausdrücken von Linked Data mit Kontext.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Format de sérialisation basé sur JSON pour exprimer les données liées avec contexte.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Formato de serialización basado en JSON para expresar datos vinculados con contexto.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: تنسيق تسلسل قائم على JSON للتعبير عن البيانات المرتبطة مع السياق.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Format serialisasi berbasis JSON untuk mengekspresikan data tertaut dengan konteks.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 用于表达带有上下文的链接数据的基于JSON的序列化格式。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: JsonLdFormaat

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: JsonLdFormat

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: FormatJsonLd

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: FormatoJsonLd

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: تنسيق JSON-LD

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: FormatJsonLd

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: JSON-LD格式

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Regelgebaseerd RDF-serialisatieformaat met één triple per regel.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Zeilenbasiertes RDF-Serialisierungsformat mit einem Triple pro Zeile.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Format de sérialisation RDF ligne par ligne avec un triple par ligne.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Formato de serialización RDF basado en líneas con un triple por línea.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: تنسيق تسلسل RDF قائم على الأسطر مع ثلاثي واحد لكل سطر.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Format serialisasi RDF berbasis baris dengan satu tigaan per baris.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 每行一个三元组的基于行的RDF序列化格式。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: NTriplesFormaat

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: NTriplesFormat

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: FormatNTriples

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: FormatoNTriples

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: تنسيق N-Triples

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: FormatNTriples

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: N-Triples格式

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: OGC-standaardformaat voor het beschrijven van sensormetadata en observatieprocessen.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: OGC-Standardformat zur Beschreibung von Sensormetadaten und Beobachtungsprozessen.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Format standard OGC pour décrire les métadonnées de capteurs et les processus d'observation.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Formato estándar OGC para describir metadatos de sensores y procesos de observación.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: تنسيق قياسي OGC لوصف بيانات تعريف المستشعر وعمليات الرصد.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Format standar OGC untuk menggambarkan metadata sensor dan proses observasi.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 用于描述传感器元数据和观测过程的OGC标准格式。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: SensorMlFormaat

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: SensorMlFormat

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: FormatSensorML

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: FormatoSensorML

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: تنسيق SensorML

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: FormatSensorML

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: SensorML格式

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Berichtpayloadformaat voor IoT-apparaatcommunicatie met behulp van het MQTT-protocol.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Nachrichtennutzlastformat für IoT-Gerätekommunikation unter Verwendung des MQTT-Protokolls.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Format de charge utile de message pour la communication d'appareils IoT utilisant le protocole MQTT.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Formato de carga útil de mensajes para comunicación de dispositivos IoT usando protocolo MQTT.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: تنسيق حمولة الرسائل لاتصال أجهزة إنترنت الأشياء باستخدام بروتوكول MQTT.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Format muatan pesan untuk komunikasi perangkat IoT menggunakan protokol MQTT.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 使用MQTT协议进行IoT设备通信的消息负载格式。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: MqttPayloadFormaat

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: MqttPayloadFormat

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: FormatPayloadMqtt

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: FormatoPayloadMqtt

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: تنسيق حمولة MQTT

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: FormatPayloadMqtt

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: MQTT负载格式

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Archiefstandaardformaat voor het coderen van inventarissen en collectiegidsen.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Archivstandardformat zur Kodierung von Findbüchern und Sammlungsführern.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Format standard d'archives pour encoder les instruments de recherche et les guides de collection.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Formato estándar archivístico para codificar guías de búsqueda y guías de colecciones.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: تنسيق قياسي أرشيفي لترميز أدلة البحث وأدلة المجموعات.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Format standar arsip untuk mengkodekan panduan pencarian dan panduan koleksi.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 用于编码检索工具和馆藏指南的档案标准格式。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: EadFormaat

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: EadFormat

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: FormatEAD

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: FormatoEAD

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: تنسيق EAD

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: FormatEAD

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: EAD格式

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Bibliotheek bibliografisch recordformaat dat MARC-gegevens codeert in XML-syntaxis.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Bibliografisches Datensatzformat, das MARC-Daten in XML-Syntax kodiert.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Format d'enregistrement bibliographique de bibliothèque encodant les données MARC en syntaxe XML.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Formato de registro bibliográfico de biblioteca que codifica datos MARC en sintaxis XML.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: تنسيق سجل ببليوغرافي لمكتبة يقوم بترميز بيانات MARC في بناء جملة XML.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Format rekaman bibliografi perpustakaan yang mengkodekan data MARC dalam sintaks XML.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 以XML语法编码MARC数据的图书馆书目记录格式。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: MarcXmlFormaat

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: MarcXmlFormat

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: FormatMarcXml

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: FormatoMarcXml

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: تنسيق MARCXML

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: FormatMarcXml

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: MARCXML格式

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Digitale bibliotheekstandaard voor het verpakken en verzenden van digitale objecten.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Digitale Bibliotheksstandard für das Verpacken und Übertragen digitaler Objekte.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Norme de bibliothèque numérique pour l'emballage et la transmission d'objets numériques.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Estándar de biblioteca digital para empaquetar y transmitir objetos digitales.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معيار مكتبة رقمية لتعبئة ونقل الكائنات الرقمية.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Standar perpustakaan digital untuk mengemas dan mentransmisikan objek digital.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 用于打包和传输数字对象的数字图书馆标准格式。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: MetsFormaat

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: MetsFormat

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: FormatMETS

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: FormatoMETS

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: تنسيق METS

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: FormatMETS

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: METS格式

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Museumgegevensoogstformaat voor het beschrijven van cultureel erfgoedobjecten.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Museumsdaten-Harvesting-Format zur Beschreibung von Kulturgütern.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Format de collecte de données muséales pour décrire les objets du patrimoine culturel.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Formato de cosecha de datos museísticos para describir objetos del patrimonio cultural.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: تنسيق حصاد بيانات المتاحف لوصف أشياء التراث الثقافي.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Format pemanenan data museum untuk menggambarkan objek warisan budaya.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 用于描述文化遗产物品的博物馆数据采集格式。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: LidoFormaat

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: LidoFormat

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: FormatLIDO

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: FormatoLIDO

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: تنسيق LIDO

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: FormatLIDO

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: LIDO格式

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Algemeen metadatanstandaard voor het beschrijven van bronnen uit verschillende domeinen.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Allzweck-Metadatenstandard zur Beschreibung domänenübergreifender Ressourcen.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Norme de métadonnées polyvalente pour décrire des ressources interdomaines.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Estándar de metadatos de propósito general para describir recursos interdominio.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معيار بيانات وصفية للأغراض العامة لوصف الموارد عبر المجالات.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Standar metadata serbaguna untuk menggambarkan sumber daya lintas domain.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 用于描述跨领域资源的通用元数据标准格式。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: DublinCoreFormaat

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: DublinCoreFormat

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: FormatDublinCore

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: FormatoDublinCore

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: تنسيق دبلن كور

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: FormatDublinCore

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 都柏林核心格式

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Vaste lay-out documentformaat dat de visuele weergave op alle platforms behoudt.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Festes Layout-Dokumentformat, das das visuelle Erscheinungsbild plattformübergreifend bewahrt.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Format de document à mise en page fixe préservant l'apparence visuelle sur toutes les plateformes.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Formato de documento de diseño fijo que preserva la apariencia visual en todas las plataformas.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: تنسيق مستند ذو تخطيط ثابت يحافظ على المظهر المرئي عبر المنصات.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Format dokumen tata letak tetap yang mempertahankan tampilan visual di semua platform.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 在所有平台上保留视觉外观的固定布局文档格式。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: PdfFormaat

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: PdfFormat

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: FormatPdf

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: FormatoPdf

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: تنسيق PDF

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: FormatPdf

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: PDF格式

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Opmaaktaalformaat voor het structureren en presenteren van webpagina-inhoud.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Auszeichnungssprachenformat zur Strukturierung und Präsentation von Webseiteninhalten.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Format de langage de balisage pour structurer et présenter le contenu des pages web.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Formato de lenguaje de marcado para estructurar y presentar contenido de páginas web.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: تنسيق لغة ترميز لهيكلة وتقديم محتوى صفحة الويب.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Format bahasa markup untuk menyusun dan menyajikan konten halaman web.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 用于构建和呈现网页内容的标记语言格式。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: HtmlFormaat

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: HtmlFormat

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: FormatHtml

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: FormatoHtml

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: تنسيق HTML

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: FormatHtml

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: HTML格式

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Ongestructureerd tekstformaat dat alleen tekengegevens zonder opmaak bevat.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Unformatiertes Textformat, das nur Zeichendaten ohne Styling enthält.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Format de texte non formaté contenant uniquement des données de caractères sans style.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Formato de texto sin formato que contiene solo datos de caracteres sin estilo.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: تنسيق نص عادي يحتوي فقط على بيانات الأحرف بدون تنسيق.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Format teks biasa yang hanya berisi data karakter tanpa gaya.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 仅包含无样式字符数据的纯文本格式。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: PlatteTekstFormaat

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: PlaintextFormat

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: FormatTexteBrut

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: FormatoTextoPlano

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: تنسيق نص عادي

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: FormatTeksBiasa

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 纯文本格式

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Onbewerkt bytesequentieformaat voor willekeurige binaire gegevensstromen.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Rohes Bytesequenzformat für beliebige binäre Datenströme.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Format de séquence d'octets brut pour flux de données binaires arbitraires.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Formato de secuencia de bytes sin procesar para flujos de datos binarios arbitrarios.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: تنسيق تسلسل بايت خام لتدفقات البيانات الثنائية العشوائية.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Format urutan byte mentah untuk aliran data biner arbitrer.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 用于任意二进制数据流的原始字节序列格式。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: BinairFormaat

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Binärformat

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: FormatBinaire

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: FormatoBinario

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: تنسيق ثنائي

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: FormatBiner

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 二进制格式

        predicate: EXACT_SYNONYM

        in_language: zh
