id: https://nde.nl/ontology/hc/class/MeasurementUnit
name: MeasurementUnit
title: Measurement Unit Class
prefixes:
  linkml: https://w3id.org/linkml/
  hc: https://nde.nl/ontology/hc/
  qudt: http://qudt.org/schema/qudt/
  schema: http://schema.org/
default_prefix: hc
imports:
- linkml:types
- ../slots/identified_by
- ../slots/has_label
- ../slots/has_description
classes:
  MeasurementUnit:
    class_uri: qudt:Unit
    description: >-
      Standardized magnitude reference used to express quantitative values in measurements and calculations.
    alt_descriptions:
      nl: >-
        Gestandaardiseerde groothedereferentie gebruikt om kwantitatieve waarden in metingen en berekeningen uit te drukken.
      de: >-
        Standardisierte Größenreferenz, die verwendet wird, um quantitative Werte in Messungen und Berechnungen auszudrücken.
      fr: >-
        Référence de grandeur normalisée utilisée pour exprimer des valeurs quantitatives dans les mesures et calculs.
      es: >-
        Referencia de magnitud estandarizada utilizada para expresar valores cuantitativos en mediciones y cálculos.
      ar: >-
        مرجع مقدار معياري يستخدم للتعبير عن القيم الكمية في القياسات والحسابات.
      id: >-
        Referensi besaran terstandarisasi yang digunakan untuk menyatakan nilai kuantitatif dalam pengukuran dan perhitungan.
      zh: >-
        用于在测量和计算中表达定量值的标准量值参考。
    structured_aliases:

      - literal_form: meeteenheid

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: eenheid

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Maßeinheit

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Einheit

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: unité de mesure

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: unité

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: unidad de medida

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: unidad

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: وحدة القياس

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: وحدة

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: satuan pengukuran

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: satuan

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 计量单位

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: 单位

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: measurement unit

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: quantification

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: dimension

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: metric

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: standard

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: qudt:Unit

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: schema:unitCode

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: identified_by

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_label

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_description

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: 'Preserved from prior description: Measurement-unit concept used for computational or digital quantification.'

        predicate: EXACT_SYNONYM

        in_language: zh
