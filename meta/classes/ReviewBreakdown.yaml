id: https://nde.nl/ontology/hc/classes/ReviewBreakdown
name: ReviewBreakdown
title: ReviewBreakdown
prefixes:
  linkml: https://w3id.org/linkml/
  hc: https://nde.nl/ontology/hc/
  schema: http://schema.org/
  prov: http://www.w3.org/ns/prov#
  xsd: http://www.w3.org/2001/XMLSchema#
imports:
  - linkml:types
default_prefix: hc
# default_range: string
classes:
  ReviewBreakdown:
    description: >-
      Distribution of user-submitted evaluations across a numerical rating scale with aggregated counts.
    alt_descriptions:
      nl: >-
        Distributie van door gebruikers ingediende beoordelingen over een numerieke beoordelingsschaal met geaggregeerde aantallen.
      de: >-
        Verteilung von Benutzerbewertungen über eine numerische Bewertungsskala mit aggregierten Zählungen.
      fr: >-
        Distribution des évaluations soumises par les utilisateurs sur une échelle de notation numérique avec des comptes agrégés.
      es: >-
        Distribución de evaluaciones enviadas por usuarios en una escala de calificación numérica con conteos agregados.
      ar: >-
        توزيع التقييمات المقدمة من المستخدمين عبر مقياس تصنيف رقمي مع counts المجمعات.
      id: >-
        Distribusi evaluasi yang dikirimkan pengguna di seluruh skala peringkat numerik dengan jumlah teragregasi.
      zh: >-
        用户提交的评价在数字评分量表上的分布及汇总计数。
    structured_aliases:

      - literal_form: beoordelingsverdeling

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: sterrenverdeling

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Bewertungsverteilung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Sternverteilung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: répartition des avis

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: distribution des étoiles

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: distribución de reseñas

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: desglose de calificaciones

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: توزيع المراجعات

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: تفصيل التقييمات

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: distribusi ulasan

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: rincian peringkat

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 评论分布

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: 评分明细

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: review breakdown

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: rating distribution

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: star ratings

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: review counts

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: evaluation summary

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Flexible mapping used by Google Maps-style review enrichment.

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Expected shape is a dictionary keyed by star rating ("1".."5") with integer counts.

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: value

        predicate: EXACT_SYNONYM

        in_language: zh
