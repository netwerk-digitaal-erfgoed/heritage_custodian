id: https://nde.nl/ontology/hc/class/Overview
name: overview_class
title: Overview Class
description: Collection of links and related resources.
prefixes:
  linkml: https://w3id.org/linkml/
  hc: https://nde.nl/ontology/hc/
  dcterms: http://purl.org/dc/terms/
  schema: http://schema.org/
  rico: https://www.ica.org/standards/RiC/ontology#
  skos: http://www.w3.org/2004/02/skos/core#
imports:
  - linkml:types
  - ../slots/has_description
  - ../slots/has_label  # was: title
  - ../slots/has_score  # was: template_specificity
  - ../slots/include
  - ../slots/retrieved_at  # was: date_retrieved
  - ../slots/has_quantity
  - ../slots/has_name
  - ../slots/has_url
  - ../slots/temporal_extent  # was: valid_from + valid_to
default_prefix: hc
# default_range: string
classes:
  Overview:
    class_uri: dcterms:Collection
    description: >-
      Curated aggregation of web links and resources captured from a shared source context with retrieval metadata.
    alt_descriptions:
      nl: >-
        Gecureerde aggregatie van weblinks en bronnen vastgelegd vanuit een gedeelde broncontext met ophaal-metadata.
      de: >-
        Kuratierte Aggregation von Weblinks und Ressourcen, die aus einem gemeinsamen Quellkontext mit Abrufmetadaten erfasst wurden.
      fr: >-
        Agrégation curatée de liens web et de ressources capturés à partir d'un contexte source partagé avec des métadonnées de récupération.
      es: >-
        Agregación curada de enlaces web y recursos capturados de un contexto de fuente compartido con metadatos de recuperación.
      ar: >-
        تجميع منسق لروابط الويب والموارد الملتقطة من سياق مصدر مشترك مع بيانات استرجاع وصفية.
      id: >-
        Agregasi terkurasi dari tautan web dan sumber daya yang diambil dari konteks sumber bersama dengan metadata pengambilan.
      zh: >-
        从共享来源上下文捕获的带有检索元数据的网页链接和资源的策划聚合。
    structured_aliases:

      - literal_form: overzicht

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: linkcollectie

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Übersicht

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Linksammlung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: aperçu

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: collection de liens

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: visión general

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: colección de enlaces

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: نظرة عامة

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: مجموعة روابط

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: ikhtisar

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: koleksi tautan

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 概览

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: 链接集合

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: overview

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: link collection

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: resource aggregation

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: web links

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: curated collection

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: "Use cases: link collections extracted from finding-aid pages, navigation pages, or resource lists."

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: "Temporal semantics: link sets can change over time; use temporal_extent and retrieved_at to qualify snapshots."

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: "Migration: introduced to replace string-valued all_links with has_overview + WebLink typed structures (see slot_fixes.yaml)."

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Collection of WebLink instances

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Used with has_overview slot

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Created from slot_fixes.yaml migration (2026-01-14)

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: dcterms:Collection

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: schema:ItemList

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: rico:RecordSet

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_name

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_label  # was: title

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_description

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: include

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_url

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: retrieved_at  # was: date_retrieved - migrated per Rule 53/56/57 (2026-01-23)

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: link_count

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: temporal_extent  # was: valid_from + valid_to

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_score  # was: template_specificity - migrated per Rule 53 (2026-01-17)

        predicate: EXACT_SYNONYM

        in_language: zh
#         range: string
      has_label:  # was: title
#         range: string
      include:
#         range: string
        multivalued: true
        inlined_as_list: false # Fixed invalid inline for primitive type
      has_url:
        range: uri
      retrieved_at:  # was: date_retrieved - migrated per Rule 53/56/57 (2026-01-23)
        range: Timestamp
        inlined: true
        examples:
          - value:
              has_timestamp: "2025-01-14"
              has_level: day
      link_count:
        range: integer
    notes:
      - |
        Preserved from prior description (commit c2cdbed6):

        Preserved from prior description (commit c2cdbed6):

        A comprehensive collection of links and resources.
        **DEFINITION**:
        An aggregation of WebLink instances representing all links or resources
        from a particular source or context. Enables structured representation
        of comprehensive link collections from web pages or other sources.
        **DUBLIN CORE ALIGNMENT**:
        Maps to `dcterms:Collection` as it represents a collection of related
        resources gathered together for a purpose.
        **TEMPORAL SEMANTICS**:
        Overview collections can change over time:
        - New links may be added
        - Existing links may be removed or updated
        - The collection scope may change
        **RELATIONSHIP TO INCLUDES_OR_INCLUDED**:
        Uses the generic `include` slot to link to WebLink
        instances, following RiC-O hierarchical patterns.
        **EXAMPLE**:
    annotations:
      custodian_types: '["*"]'
      custodian_types_rationale: >-
        Applicable to all heritage custodian types as comprehensive
        link collections are universal navigation patterns.
      custodian_types_primary: "*"
      specificity_score: 0.30
      specificity_rationale: >-
        Low specificity - broadly applicable for link collection
        and navigation modeling.
    see_also:
      - http://purl.org/dc/terms/Collection
      - http://schema.org/ItemList
      - https://example.org/civil-registry
      - https://example.org/notarial
    examples:
      - value:
          id: hc:overview/findingaid-001-links
          source_url: https://www.nationaalarchief.nl/onderzoeken/zoekhulpen/familiegeschiedenis
          retrieved_at:  # was: date_retrieved
            has_timestamp: "2025-01-14"
            has_level: day
          link_count: 15
      - value: |
          overview:
          id: hc:overview/findingaid-familyhistory-links
          title: "Family History Research Links"
          description: "All links from the family history finding aid page"
          source_url: https://www.nationaalarchief.nl/onderzoeken/zoekhulpen/familiegeschiedenis
          retrieved_at:
            has_timestamp: "2025-01-14"
            has_level: day
          include:
            - id: hc:link/civil-registry
              url: https://example.org/civil-registry
              link_text: "Civil Registry"
            - id: hc:link/notarial-records
              url: https://example.org/notarial
              link_text: "Notarial Records"
          # REMOVED inline slots 2026-01-16 - Rule 48 violation
          # Slots are imported from ../slots/ - do not define inline
