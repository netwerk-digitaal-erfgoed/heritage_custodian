id: https://nde.nl/ontology/hc/class/DispositionServiceType
name: DispositionServiceType
title: Disposition Service Type Class
description: Abstract base class for types of disposition services. MIGRATED from disposition_service string (2026-01-26).
prefixes:
  hc: https://nde.nl/ontology/hc/
  skos: http://www.w3.org/2004/02/skos/core#
imports:
  - linkml:types
  - ../slots/has_description
  - ../slots/has_label
default_prefix: hc
classes:
  DispositionServiceType:
    abstract: true
    class_uri: skos:Concept
    description: >-
      Abstract classification for end-of-lifecycle processing services applied to records and materials.
    alt_descriptions:
      nl: >-
        Abstracte classificatie voor einde-levenscyclus verwerkingsdiensten toegepast op records en materialen.
      de: >-
        Abstrakte Klassifikation für End-of-Life-Verarbeitungsdienste, die auf Datensätze und Materialien angewendet werden.
      fr: >-
        Classification abstraite pour les services de traitement de fin de cycle de vie appliqués aux documents et matériaux.
      es: >-
        Clasificación abstracta para servicios de procesamiento de fin de ciclo de vida aplicados a registros y materiales.
      ar: >-
        تصنيف تجريدي لخدمات معالجة نهاية دورة الحياة المطبقة على السجلات والمواد.
      id: >-
        Klasifikasi abstrak untuk layanan pemrosesan akhir masa pakai yang diterapkan pada catatan dan materi.
      zh: >-
        应用于记录和材料的生命周期终结处理服务的抽象分类。
    structured_aliases:

      - literal_form: afvoerdiensttype

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: beschikkingsservice

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Entsorgungsdiensttyp

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Dispositionsdienst

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: type de service d'élimination

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: service de disposition

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: tipo de servicio de disposición

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: servicio de eliminación

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: نوع خدمة التخلص

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: خدمة التصرف

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: jenis layanan disposisi

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: layanan pembuangan

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 处置服务类型

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: 处理服务

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: disposition

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: records management

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: lifecycle

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: retention

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: destruction

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: transfer

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: skos:Concept

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_label

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_description

        predicate: EXACT_SYNONYM

        in_language: zh
