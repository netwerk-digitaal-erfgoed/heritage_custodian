id: https://nde.nl/ontology/hc/class/LoadingDock
name: LoadingDock
title: Loading Dock Class
prefixes:
  linkml: https://w3id.org/linkml/
  hc: https://nde.nl/ontology/hc/
  schema: http://schema.org/
default_prefix: hc
imports:
- linkml:types
- ../slots/has_description
classes:
  LoadingDock:
    class_uri: schema:LocationFeatureSpecification
    description: >-
      Designated area for receiving and dispatching physical items through vehicular transport.
    alt_descriptions:
      nl: >-
        Aangewezen gebied voor het ontvangen en verzenden van fysieke objecten via voertuigtransport.
      de: >-
        Ausgewiesener Bereich für den Empfang und Versand physischer Gegenstände durch Fahrzeugtransport.
      fr: >-
        Zone désignée pour la réception et l'expédition d'objets physiques par transport vehiculaire.
      es: >-
        Área designada para recibir y despachar objetos físicos mediante transporte vehicular.
      ar: >-
        منطقة مخصصة لاستقبال وإرسال الأشياء المادية عبر النقل المركبات.
      id: >-
        Area yang ditunjuk untuk menerima dan mengirim benda fisik melalui transportasi kendaraan.
      zh: >-
        通过车辆运输接收和发送物理物品的指定区域。
    structured_aliases:

      - literal_form: laadperron

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: loskaai

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Laderampe

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Verladedock

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: quai de chargement

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: zone de chargement

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: muelle de carga

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: plataforma de carga

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: رصيف التحميل

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: منطقة الشحن

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: dermaga muat

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: area pemuatan

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 装卸月台

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: 货运码头

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: logistics

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: loading bay

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: freight

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: delivery

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: shipping

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: receiving

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: 'Preserved from prior description: Loading and logistics dock facility used for object transfer.'

        predicate: EXACT_SYNONYM

        in_language: zh
