id: https://nde.nl/ontology/hc/class/LabelTypes
name: LabelTypes
title: Label Types
prefixes:
  linkml: https://w3id.org/linkml/
  hc: https://nde.nl/ontology/hc/
  skos: http://www.w3.org/2004/02/skos/core#
default_prefix: hc
imports:
  - ./LabelType
  - linkml:types
classes:
  ShortName:
    is_a: LabelType
    class_uri: hc:ShortName
    description: >-
      Label category designating abbreviated or compact forms used for brevity in references.
    alt_descriptions:
      nl: Labelcategorie voor afgekorte of compacte vormen die worden gebruikt voor beknopte verwijzingen.
      de: Labelkategorie für abgekürzte oder kompakte Formen, die für kurze Referenzen verwendet werden.
      fr: Catégorie d'étiquette désignant des formes abrégées ou compactes utilisées pour la concision.
      es: Categoría de etiqueta que designa formas abreviadas o compactas para referencias breves.
      ar: فئة تسمية تحدد أشكالاً مختصرة أو مضغوطة للاستخدام في الإشارات الموجزة.
      id: Kategori label yang menunjukkan bentuk yang disingkat atau ringkas untuk referensi singkat.
      zh: 标签类别，指定用于简短引用的缩写或紧凑形式。
    structured_aliases:

      - literal_form: KorteNaam

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Kurzname

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: NomCourt

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: NombreCorto

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: اسم مختصر

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: NamaPendek

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 简称

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Labelcategorie voor alfanumerieke identificaties voor organisatorische subeenheden.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Labelkategorie für alphanumerische Kennzeichner für organisationale Untereinheiten.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Catégorie d'étiquette désignant des identifiants alphanumériques pour les sous-unités organisationnelles.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Categoría de etiqueta que designa identificadores alfanuméricos para subunidades organizativas.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: فئة تسمية تحدد معرفات أبجدية رقمية للوحدات الفرعية التنظيمية.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Kategori label yang menunjukkan pengidentifikasi alfanumerik untuk sub-unit organisasi.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 标签类别，指定组织子单元的字母数字标识符。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Afdelingscode

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Abteilungscode

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: CodeDépartement

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: CódigoDepartamento

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: رمز القسم

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: KodeDepartemen

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 部门代码

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Labelcategorie voor weergavenamen voor organisatorische subeenheden.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Labelkategorie für Anzeigenamen für organisationale Untereinheiten.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Catégorie d'étiquette désignant des noms d'affichage pour les sous-unités organisationnelles.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Categoría de etiqueta que designa nombres para mostrar para subunidades organizativas.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: فئة تسمية تحدد أسماء العرض للوحدات الفرعية التنظيمية.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Kategori label yang menunjukkan nama tampilan untuk sub-unit organisasi.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 标签类别，指定组织子单元的显示名称。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Afdelingsnaam

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Abteilungsname

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: NomDépartement

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: NombreDepartamento

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: اسم القسم

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: NamaDepartemen

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 部门名称

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Labelcategorie voor formeel geregistreerde of wettelijk erkende aanduidingen.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Labelkategorie für formell eingetragene oder rechtlich anerkannte Bezeichnungen.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Catégorie d'étiquette désignant des dénominations formellement enregistrées ou légalement reconnues.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Categoría de etiqueta que designa denominaciones formalmente registradas o legalmente reconocidas.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: فئة تسمية تحدد التسميات المسجلة رسمياً أو المعترف بها قانونياً.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Kategori label yang menunjukkan sebutan yang terda secara resmi atau diakui secara hukum.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 标签类别，指定正式注册或法律认可的名称。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: OfficieelLabel

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: OffiziellesLabel

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: ÉtiquetteOfficielle

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: EtiquetaOficial

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: تسمية رسمية

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: LabelResmi

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 官方标签

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Labelcategorie voor verkorte vormen en acroniemen voor compacte verwijzing.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Labelkategorie für verkürzte Formen und Akronyme für kompakte Referenz.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Catégorie d'étiquette désignant des formes raccourcies et des acronymes pour référence compacte.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Categoría de etiqueta que designa formas abreviadas y acrónimos para referencia compacta.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: فئة تسمية تحدد الأشكال المختصرة والاختصارات للإشارة المختصرة.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Kategori label yang menunjukkan bentuk yang dipersingkat dan akronim untuk referensi ringkas.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 标签类别，指定用于紧凑引用的缩写形式和首字母缩略词。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Labelafkorting

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Labelabkürzung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: AbréviationÉtiquette

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: AbreviaturaEtiqueta

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: اختصار التسمية

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: SingkatanLabel

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 标签缩写

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Labelcategorie voor niet-openbare referentie-ID's voor intern gebruik.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Labelkategorie für nicht-öffentliche Referenzkennzeichner zur internen Verwendung.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Catégorie d'étiquette désignant des identifiants de référence non publics pour usage interne.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Categoría de etiqueta que designa identificadores de referencia no públicos para uso interno.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: فئة تسمية تحدد معرفات مرجعية غير عامة للاستخدام الداخلي.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Kategori label yang menunjukkan pengidentifikasi referensi non-publik untuk penggunaan internal.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 标签类别，指定供内部使用的非公开参考标识符。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: InterneCode

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: InternerCode

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: CodeInterne

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: CódigoInterno

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: رمز داخلي

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: KodeInternal

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 内部代码

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Labelcategorie voor namen die bedoeld zijn voor publieksgerichte presentatiecontexten.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Labelkategorie für Namen, die für öffentliche Präsentationskontexte bestimmt sind.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Catégorie d'étiquette désignant des noms destinés aux contextes de présentation publique.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Categoría de etiqueta que designa nombres para contextos de presentación面向 el público.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: فئة تسمية تحدد أسماء مخصصة لسياقات العرض العامة.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Kategori label yang menunjukkan nama yang dimaksudkan untuk konteks presentasi menghadap publik.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 标签类别，指定用于面向公众展示语境的名称。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Weergavenaam

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Anzeigename

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: NomAffichage

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: NombreParaMostrar

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: اسم العرض

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: NamaTampilan

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 显示名称

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Labelcategorie voor zelfaangeduide namen vanuit insider- of gemeenschapsperspectieven.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Labelkategorie für selbstbezeichnete Namen aus Insider- oder Gemeinschaftsperspektiven.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Catégorie d'étiquette désignant des noms auto-désignés selon les perspectives internes ou communautaires.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Categoría de etiqueta que designa nombres auto-designados desde perspectivas internas o comunitarias.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: فئة تسمية تحدد الأسماء الذاتية من منظور الداخل أو المجتمع.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Kategori label yang menunjukkan nama yang ditunjuk sendiri dari perspektif orang dalam atau komunitas.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 标签类别，指定来自内部或社区视角的自我指定名称。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: EmischLabel

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: EmischesLabel

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Étiquetteémique

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: EtiquetaÉmica

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: تسمية ذاتية

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: LabelEmic

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 内部标签

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Labelcategorie voor aanvullende namen, aliassen of variantvormen naast de primaire aanduiding.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Labelkategorie für zusätzliche Namen, Aliasse oder Variantenformen über die primäre Bezeichnung hinaus.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Catégorie d'étiquette désignant des noms supplémentaires, alias ou formes alternatives au-delà de la désignation principale.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Categoría de etiqueta que designa nombres adicionales, alias o formas variantes más allá de la designación primaria.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: فئة تسمية تحدد أسماء إضافية أو أسماء مستعارة أو أشكال متغيرة بما يتجاوز التسمية الأساسية.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Kategori label yang menunjukkan nama tambahan, alias, atau bentuk varian di luar sebutan utama.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 标签类别，指定主要名称之外的附加名称、别名或变体形式。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: AlternatieveNaam

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Alternativname

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: NomAlternatif

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: NombreAlternativo

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: اسم بديل

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: NamaAlternatif

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 替代名称

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Labelcategorie voor namen die direct uit bronevidentie zijn vastgelegd zonder normalisatie.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Labelkategorie für Namen, die direkt aus Quellenbeweisen ohne Normalisierung erfasst wurden.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Catégorie d'étiquette désignant des noms capturés directement à partir de preuves sources sans normalisation.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Categoría de etiqueta que designa nombres capturados directamente de evidencia fuente sin normalización.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: فئة تسمية تحدد أسماء تم التقاطها مباشرة من أدلة المصدر دون تسوية.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Kategori label yang menunjukkan nama yang diambil langsung dari bukti sumber tanpa normalisasi.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 标签类别，指定直接从源证据捕获而未经标准化的名称。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: WaargenomenNaam

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: BeobachteterName

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: NomObservé

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: NombreObservado

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: اسم ملاحظ

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: NamaYangDiamati

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 观察到的名称

        predicate: EXACT_SYNONYM

        in_language: zh
