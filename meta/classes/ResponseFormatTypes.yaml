id: https://nde.nl/ontology/hc/class/ResponseFormatTypes
name: ResponseFormatTypes
prefixes:
  linkml: https://w3id.org/linkml/
  hc: https://nde.nl/ontology/hc/
  skos: http://www.w3.org/2004/02/skos/core#
imports:
- ./ResponseFormatType
- linkml:types
classes:
  JSONFormat:
    is_a: ResponseFormatType
    class_uri: hc:JSONFormat
    description: JSON format.
    alt_descriptions:
      nl: JSON-formaat.
      de: JSON-Format.
    structured_aliases:
    - literal_form: JSON-formaat
      in_language: nl
    - literal_form: JSON-Format
      in_language: de
    broad_mappings:
    - skos:Concept
    annotations:
      specificity_score: 0.1
      specificity_rationale: Generic utility class/slot created during migration
      custodian_types: "['*']"
  XMLFormat:
    is_a: ResponseFormatType
    class_uri: hc:XMLFormat
    description: XML format.
    alt_descriptions:
      nl: XML-formaat.
      de: XML-Format.
    structured_aliases:
    - literal_form: XML-formaat
      in_language: nl
    - literal_form: XML-Format
      in_language: de
    broad_mappings:
    - skos:Concept
  RDFFormat:
    is_a: ResponseFormatType
    class_uri: hc:RDFFormat
    description: RDF format.
    alt_descriptions:
      nl: RDF-formaat.
      de: RDF-Format.
    structured_aliases:
    - literal_form: RDF-formaat
      in_language: nl
    - literal_form: RDF-Format
      in_language: de
    broad_mappings:
    - skos:Concept
  HTMLFormat:
    is_a: ResponseFormatType
    class_uri: hc:HTMLFormat
    description: HTML format.
    alt_descriptions:
      nl: HTML-formaat.
      de: HTML-Format.
    structured_aliases:
    - literal_form: HTML-formaat
      in_language: nl
    - literal_form: HTML-Format
      in_language: de
    broad_mappings:
    - skos:Concept
  CSVFormat:
    is_a: ResponseFormatType
    class_uri: hc:CSVFormat
    description: CSV format.
    alt_descriptions:
      nl: CSV-formaat.
      de: CSV-Format.
    structured_aliases:
    - literal_form: CSV-formaat
      in_language: nl
    - literal_form: CSV-Format
      in_language: de
    broad_mappings:
    - skos:Concept
