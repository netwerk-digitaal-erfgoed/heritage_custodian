id: https://nde.nl/ontology/hc/class/GenerationEvent
name: GenerationEvent
title: Generation Event Class
prefixes:
  linkml: https://w3id.org/linkml/
  hc: https://nde.nl/ontology/hc/
  prov: http://www.w3.org/ns/prov#
  schema: http://schema.org/
default_prefix: hc
imports:
  - linkml:types
  - ../classes/Provenance
  - ../classes/ConfidenceScore
  - ../slots/temporal_extent
  - ../slots/has_provenance
  - ../slots/has_description
  - ../slots/has_score
classes:
  GenerationEvent:
    class_uri: prov:Generation
    description: >-
      Activity in which an entity comes into existence through creation,
      derivation, or transformation processes.
    alt_descriptions:
      nl: >-
        Activiteit waarbij een entiteit tot stand komt door creatie-, afleiding-
        of transformatieprocessen.
      de: >-
        Aktivität, bei der eine Entität durch Erstellungs-, Ableitungs- oder
        Transformationsprozesse entsteht.
      fr: >-
        Activité par laquelle une entité prend naissance par des processus de
        création, de dérivation ou de transformation.
      es: >-
        Actividad en la que una entidad llega a existir mediante procesos de
        creación, derivación o transformación.
      ar: >-
        نشاط تنشأ فيه كيان من خلال عمليات الخلق أو الاشتقاق أو التحول.
      id: >-
        Aktivitas di mana entitas hadir melalui proses penciptaan, derivasi, atau transformasi.
      zh: >-
        通过创建、派生或转换过程使实体产生的活动。
    structured_aliases:

      - literal_form: generatiegebeurtenis

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: productie-occurentie

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Generierungsereignis

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Produktionsvorgang

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: événement de génération

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: occurrence de production

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: evento de generación

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: ocurrencia de producción

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: حدث التوليد

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: مناسبة الإنتاج

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: peristiwa pembuatan

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: kejadian produksi

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 生成事件

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: 产生发生

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: generation event

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: creation occurrence

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: production event

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: entity creation

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: data generation

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: prov:Generation

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: schema:CreateAction

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: temporal_extent

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_provenance

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_description

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_score

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: |

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Video chapter generation (manual, AI, imported)

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Content extraction events

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Automated processing activities

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Confidence scoring for generated content

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: "temporal_extent: When the generation occurred (TimeSpan)"

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: "has_provenance: Who/what performed the generation (Provenance)"

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: "has_description: Details about the generation process"

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: "has_score: Confidence score for the generated content (ConfidenceScore)"

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Maps to prov:Generation (PROV-O generation event)

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Also maps to schema:CreateAction (Schema.org action)

        predicate: EXACT_SYNONYM

        in_language: zh
