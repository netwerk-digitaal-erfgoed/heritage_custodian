id: https://nde.nl/ontology/hc/class/SocialMediaPostType
name: SocialMediaPostType
title: Social Media Post Type
prefixes:
  linkml: https://w3id.org/linkml/
  hc: https://nde.nl/ontology/hc/
  schema: http://schema.org/
  dcterms: http://purl.org/dc/terms/
  skos: http://www.w3.org/2004/02/skos/core#
  as: https://www.w3.org/ns/activitystreams#
  wd: http://www.wikidata.org/entity/
default_prefix: hc
imports:
- linkml:types
- ../enums/PostTypeCategoryEnum
- ../metadata
- ../slots/has_score
- ../slots/categorized_as
- ../slots/has_description
- ../slots/ephemeral
- ../slots/has_use_case
- ../slots/identified_by
- ../slots/has_time_interval
- ../slots/has_format
- ../slots/has_label
- ../slots/applicable_in
classes:
  SocialMediaPostType:
    class_uri: skos:Concept
    abstract: true
    description: >-
      Abstract taxonomy concept for categorizing social media content formats and publication types.
    alt_descriptions:
      nl: >-
        Abstract taxonomieconcept voor het categoriseren van sociale media-inhoudsformaten en publicatietypes.
      de: >-
        Abstraktes Taxonomiekonzept zur Kategorisierung von Social-Media-Inhaltsformaten und Veröffentlichungstypen.
      fr: >-
        Concept taxonomique abstrait pour catégoriser les formats de contenu des médias sociaux et les types de publication.
      es: >-
        Concepto taxonómico abstracto para categorizar formatos de contenido de redes sociales y tipos de publicación.
      ar: >-
        مفهوم تصنيفي تجريدي لتصنيف تنسيقات محتوى وسائل التواصل الاجتماعي وأنواع النشر.
      id: >-
        Konsep taksonomi abstrak untuk mengkategorikan format konten media sosial dan jenis publikasi.
      zh: >-
        用于分类社交媒体内容格式和发布类型的抽象分类概念。
    structured_aliases:

      - literal_form: social media berichttype

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: postformaat

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Social-Media-Beitragstyp

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Postformat

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: type de post réseaux sociaux

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: format de publication

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: tipo de post en redes sociales

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: formato de publicación

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: نوع منشور وسائل التواصل

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: تنسيق النشر

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: jenis posting media sosial

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: format postingan

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 社交媒体帖子类型

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: 发布格式

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: social media post type

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: content format

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: publication type

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: media category

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: post format

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: as:Object

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: schema:MediaObject

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: identified_by

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_label

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_description

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: categorized_as

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: applicable_in

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_format

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_time_interval

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: ephemeral

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_use_case

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_score

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Abstract base class; use concrete subclasses in `schemas/20251121/linkml/modules/classes/SocialMediaPostTypes.yaml`.

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Represents post TYPE concepts (formats), not individual SocialMediaPost instances.

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: A single post can legitimately have multiple post types (e.g., carousel containing both images and videos).

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Activity Streams 2.0 terms (as:Video, as:Image, as:Note, as:Audio, as:Article) provide interoperability targets.

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: https://www.w3.org/TR/activitystreams-vocabulary/

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: https://www.w3.org/ns/activitystreams#Object

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: skos:Concept

        predicate: EXACT_SYNONYM

        in_language: zh
