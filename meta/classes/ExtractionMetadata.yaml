id: https://nde.nl/ontology/hc/class/ExtractionMetadata
name: ExtractionMetadata
title: Extraction Metadata Class
prefixes:
  linkml: https://w3id.org/linkml/
  hc: https://nde.nl/ontology/hc/
  schema: http://schema.org/
  prov: http://www.w3.org/ns/prov#
  dct: http://purl.org/dc/terms/
default_prefix: hc
imports:
- linkml:types
- ../enums/ProfileExtractionMethodEnum
- ../metadata
- ../slots/has_source
- ../slots/identified_by
- ../slots/retrieved_at
- ../slots/retrieved_by
- ../slots/has_method
- ../slots/has_url
- ../slots/has_expense
- ../slots/has_provenance
- ../slots/has_score
classes:
  ExtractionMetadata:
    class_uri: prov:Activity
    description: >-
      Provenance record documenting the method, timing, agent, and source involved in harvesting data from external systems.
    alt_descriptions:
      nl: >-
        Herkomstrecord dat de methode, timing, agent en bron documenteert die betrokken zijn bij het ophalen van gegevens uit externe systemen.
      de: >-
        Herkunftsdatensatz, der Methode, Zeitpunkt, Akteur und Quelle dokumentiert, die an der Extraktion von Daten aus externen Systemen beteiligt sind.
      fr: >-
        Enregistrement de provenance documentant la méthode, le timing, l'agent et la source impliqués dans l'extraction de données de systèmes externes.
      es: >-
        Registro de procedencia que documenta el método, tiempo, agente y fuente involucrados en la extracción de datos de sistemas externos.
      ar: >-
        سجل المنشأ الذي يوثق الطريقة والتوقيت والوكيل والمصدر المشاركين في استخراج البيانات من الأنظمة الخارجية.
      id: >-
        Catatan asal-usul yang mendokumentasikan metode, waktu, agen, dan sumber yang terlibat dalam ekstraksi data dari sistem eksternal.
      zh: >-
        记录从外部系统提取数据所涉及的方法、时间、代理和来源的来源记录。
    structured_aliases:

      - literal_form: extractiemetadata

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: ophaalprovenance

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Extraktionsmetadaten

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Abrufherkunft

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: métadonnées d'extraction

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: provenance d'extraction

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: metadatos de extracción

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: procedencia de extracción

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: بيانات الاستخراج الوصفية

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: منشأ الاستخراج

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: metadata ekstraksi

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: asal-usul ekstraksi

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 提取元数据

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: 提取来源

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: extraction metadata

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: provenance

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: data harvesting

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: retrieval record

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: ingestion

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: prov:Activity

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: schema:Action

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: dct:ProvenanceStatement

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_source

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: identified_by

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: retrieved_at

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: retrieved_by

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_method

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_url

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_expense

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_provenance

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_score

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: https://www.linkedin.com/in/...\

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: |

        predicate: EXACT_SYNONYM

        in_language: zh
