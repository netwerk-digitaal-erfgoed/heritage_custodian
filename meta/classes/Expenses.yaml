id: https://nde.nl/ontology/hc/class/Expenses
name: Expenses
title: Expenses Class
prefixes:
  linkml: https://w3id.org/linkml/
  hc: https://nde.nl/ontology/hc/
  schema: http://schema.org/
  frapo: http://purl.org/cerif/frapo/
default_prefix: hc
imports:
  - linkml:types
  - ../enums/ExpenseTypeEnum
  - ../slots/has_type
  - ../slots/has_quantity
  - ../slots/has_description
  - ../slots/temporal_extent
classes:
  Expenses:
    class_uri: frapo:Expenditure
    description: >-
      Classified financial outflow entry documenting monetary expenditure within a custodial reporting period.
    alt_descriptions:
      nl: >-
        Geclassificeerde financiële uitgaveregistratie die monetaire uitgaven binnen een beheerrapporteringsperiode documenteert.
      de: >-
        Klassifizierter Finanzabfluss-Eintrag, der monetäre Ausgaben innerhalb eines Bewahrungsberichtzeitraums dokumentiert.
      fr: >-
        Entrée de sortie financière classée documentant les dépenses monétaires au cours d'une période de déclaration de garde.
      es: >-
        Entrada de salida financiera clasificada que documenta los gastos monetarios dentro de un período de presentación de informes de custodia.
      ar: >-
        قيد التدفق المالي المصنف الذي يوثق النفقات النقدية خلال فترة إعداد تقارير الحضانة.
      id: >-
        Entri aliran keuangan terklasifikasi yang mendokumentasikan pengeluaran moneter dalam periode pelaporan pengelolaan.
      zh: >-
        在保管报告期内记录货币支出的分类财务流出条目。
    structured_aliases:

      - literal_form: uitgaven

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: lasten

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Ausgaben

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Aufwendungen

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: dépenses

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: charges

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: gastos

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: egresos

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: المصروفات

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: النفقات

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: pengeluaran

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: beban

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 支出

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: 费用

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: expenses

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: expenditure

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: financial outflow

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: spending

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: costs

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: frapo:Expenditure

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: schema:MonetaryAmount

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_type

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_quantity

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_description

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: temporal_extent

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: "value: |"

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: "expense_type: ADMINISTRATIVE"

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: "expense_type: PROGRAM"

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: |

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: No standard ontology has a direct "expense" predicate

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Uses FRAPO (Funding, Research Administration and Projects Ontology) patterns

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Aligns with schema:MonetaryAmount for monetary values

        predicate: EXACT_SYNONYM

        in_language: zh
