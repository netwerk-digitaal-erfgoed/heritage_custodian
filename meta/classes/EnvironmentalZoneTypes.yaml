id: https://nde.nl/ontology/hc/class/EnvironmentalZoneTypes
name: EnvironmentalZoneTypes
title: Environmental Zone Types
prefixes:
  linkml: https://w3id.org/linkml/
  hc: https://nde.nl/ontology/hc/
  skos: http://www.w3.org/2004/02/skos/core#
default_prefix: hc
imports:
  - ./EnvironmentalZoneType
  - linkml:types
  - ../slots/has_code
  - ../slots/has_score
  - ../slots/has_setpoint
  - ../slots/has_light_exposure
  - ../slots/maximum_of_maximum
classes:
  ArchiveStandardStorageEnvironment:
    is_a: EnvironmentalZoneType
    class_uri: hc:ArchiveStandardStorageEnvironment
    description: >-
      Climate profile for stable paper-based documentary storage with moderate temperature and humidity parameters.
    alt_descriptions:
      nl: Klimaatprofiel voor stabiele papiergebaseerde documentaire opslag met gematigde temperatuur- en vochtigheidsparameters.
      de: Klimaprofil für stabile papierbasierte Dokumentenaufbewahrung mit moderaten Temperatur- und Feuchtigkeitsparametern.
      fr: Profil climatique pour le stockage stable de documents papier avec des paramètres de température et d'humidité modérés.
      es: Perfil climático para almacenamiento estable de documentos en papel con parámetros moderados de temperatura y humedad.
      ar: ملف مناخي للتخزين المستقر للمستندات الورقية مع معلمات معتدلة لدرجة الحرارة والرطوبة.
      id: Profil iklim untuk penyimpanan dokumen berbasis kertas yang stabil dengan parameter suhu dan kelembaban sedang.
      zh: 具有适度温度和湿度参数的稳定纸质文档存储气候概况。
    structured_aliases:

      - literal_form: ArchiefStandaardOpslagOmgeving

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: ArchivStandardLagerUmgebung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: EnvironnementStockageArchivesStandard

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: AmbienteAlmacenamientoArchivoEstándar

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: بيئة تخزين الأرشيف القياسية

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: LingkunganPenyimpananArsipStandar

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 档案标准存储环境

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Klimaatprofiel voor gekoelde omstandigheden gebruikt om chemische achteruitgang in gevoelige materialen te vertragen.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Klimaprofil für gekühlte Bedingungen zur Verlangsamung des chemischen Verfalls empfindlicher Materialien.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Profil climatique pour conditions réfrigérées utilisées pour ralentir la détérioration chimique des matériaux sensibles.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Perfil climático para condiciones refrigeradas utilizadas para retardar el deterioro químico de materiales sensibles.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: ملف مناخي للظروف المبردة المستخدمة لإبطاء التدهور الكيميائي للمواد الحساسة.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Profil iklim untuk kondisi pendingin digunakan untuk memperlambat kerusakan kimia pada bahan sensitif.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 用于减缓敏感材料化学劣化的冷藏条件气候概况。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: KoudeOpslagOmgeving

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: KaltlagerUmgebung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: EnvironnementStockageFroid

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: AmbienteAlmacenamientoFrío

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: بيئة التخزين البارد

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: LingkunganPenyimpananDingin

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 冷藏存储环境

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Klimaatprofiel voor onderkoelde omstandigheden die langdurige conserveringsstabilisatie mogelijk maken.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Klimaprofil für unterkühlte Bedingungen zur langfristigen Erhaltungsstabilisierung.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Profil climatique pour conditions sous-zéro permettant la stabilisation de préservation à long terme.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Perfil climático para condiciones bajo cero que permiten la estabilización de preservación a largo plazo.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: ملف مناخي للظروف تحت الصفر لتمكين استقرار الحفظ طويل المدى.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Profil iklim untuk kondisi di bawah nol yang memungkinkan stabilisasi pelestarian jangka panjang.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 实现长期保存稳定的零下条件气候概况。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: VriezerOpslagOmgeving

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: GefrierlagerUmgebung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: EnvironnementStockageCongelé

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: AmbienteAlmacenamientoCongelado

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: بيئة التخزين المجمد

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: LingkunganPenyimpananBeku

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 冷冻存储环境

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Klimaatprofiel geoptimaliseerd voor fotografische materialen met strikte temperatuur-, vochtigheids- en lichtcontroles.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Klimaprofil optimiert für fotografische Materialien mit strengen Temperatur-, Feuchtigkeits- und Lichtkontrollen.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Profil climatique optimisé pour les matériaux photographiques avec des contrôles rigoureux de température, d'humidité et de lumière.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Perfil climático optimizado para materiales fotográficos con controles estrictos de temperatura, humedad y luz.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: ملف مناخي محسن للمواد الفوتوغرافية مع ضوابط صارمة لدرجة الحرارة والرطوبة والضوء.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Profil iklim yang dioptimalkan untuk materi fotografi dengan kontrol suhu, kelembaban, dan cahaya yang ketat.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 针对摄影材料优化的具有严格温度、湿度和光照控制的气候概况。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: FotografischeOpslagOmgeving

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: FotografischeLagerUmgebung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: EnvironnementStockagePhotographique

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: AmbienteAlmacenamientoFotográfico

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: بيئة التخزين الفوتوغرافي

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: LingkunganPenyimpananFotografi

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 摄影存储环境

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Klimaatprofiel voor textielcollecties met nadruk op milieuconstantheid en cumulatieve lichtlimieten.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Klimaprofil für Stoffsammlungen mit Schwerpunkt auf Umweltstabilität und kumulativen Lichtgrenzen.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Profil climatique pour collections textiles mettant l'accent sur la stabilité environnementale et les limites cumulatives de lumière.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Perfil climático para colecciones textiles que enfatiza la estabilidad ambiental y los límites acumulativos de luz.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: ملف مناخي لمجموعات الأقمشة مع التركيز على الاستقرار البيئي وحدود الضوء التراكمية.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Profil iklim untuk koleksi tekstil yang menekankan stabilitas lingkungan dan batas cahaya kumulatif.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 强调环境稳定性和累积光照限制的织物馆藏气候概况。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: TextielOpslagOmgeving

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: TextilLagerUmgebung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: EnvironnementStockageTextile

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: AmbienteAlmacenamientoTextil

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: بيئة التخزين النسيجي

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: LingkunganPenyimpananTekstil

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 纺织品存储环境

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Klimaatprofiel voor kunstcollecties waar minimale milieufluctuatietolerantie prioriteit heeft.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Klimaprofil für Kunstbestände, bei dem minimale Umgebungsschwankungstoleranz priorisiert wird.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Profil climatique pour collections d'art où la tolérance minimale aux fluctuations environnementales est priorisée.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Perfil climático para colecciones de arte donde se prioriza la tolerancia mínima a fluctuaciones ambientales.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: ملف مناخي للمقتنيات الفنية حيث يتم إعطاء الأولوية للحد الأدنى من التسامح مع التقلبات البيئية.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Profil iklim untuk koleksi seni rupa di mana toleransi fluktuasi lingkungan minimal diprioritaskan.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 优先考虑最低环境波动容忍度的艺术品馆藏气候概况。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: KunstOpslagOmgeving

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: KunstLagerUmgebung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: EnvironnementStockageArt

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: AmbienteAlmacenamientoArte

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: بيئة التخزين الفني

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: LingkunganPenyimpananSeni

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 艺术品存储环境

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Klimaatprofiel gericht op corrosiepreventie door gecontroleerde lage luchtvochtigheidsopslagomstandigheden.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Klimaprofil zur Korrosionsverhütung durch kontrollierte Lagerbedingungen mit niedriger Luftfeuchtigkeit.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Profil climatique ciblant la prévention de la corrosion par des conditions de stockage à faible humidité contrôlée.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Perfil climático dirigido a la prevención de corrosión mediante condiciones de almacenamiento de baja humedad controlada.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: ملف مناخي يستهدف منع التآكل من خلال ظروف تخزين منخفضة الرطوبة مضبوطة.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Profil iklim yang menargetkan pencegahan korosi melalui kondisi penyimpanan kelembaban rendah yang terkontrol.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 通过受控低湿度存储条件实现防腐目标的气候概况。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: MetaalobjectOpslagOmgeving

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: MetallgegenstandLagerUmgebung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: EnvironnementStockageObjetsMétal

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: AmbienteAlmacenamientoObjetosMetal

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: بيئة تخزين الأشياء المعدنية

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: LingkunganPenyimpananBendaLogam

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 金属物品存储环境

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Klimaatprofiel voor biologische en geologische specimens met gedifferentieerde behandeling volgens type specimen.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Klimaprofil für biologische und geologische Exemplare mit differenzierter Behandlung je nach Exemplartyp.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Profil climatique pour spécimens biologiques et géologiques avec traitement différencié selon le type de spécimen.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Perfil climático para especímenes biológicos y geológicos con tratamiento diferenciado según el tipo de espécimen.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: ملف مناخي للعينات البيولوجية والجيولوجية مع معاملة متمايزة حسب نوع العينة.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Profil iklim untuk spesimen biologis dan geologis dengan perlakuan yang dibedakan sesuai jenis spesimen.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 根据标本类型进行差异化处理的生物和地质标本气候概况。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: NatuurhistorieOpslagOmgeving

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: NaturgeschichteLagerUmgebung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: EnvironnementStockageHistoireNaturelle

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: AmbienteAlmacenamientoHistoriaNatural

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: بيئة تخزين التاريخ الطبيعي

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: LingkunganPenyimpananSejarahAlam

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 自然历史存储环境

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Klimaatprofiel voor gebonden werken met omstandigheden geoptimaliseerd voor papier- en bandduurzaamheid.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Klimaprofil für gebundene Bände mit Bedingungen optimiert für Papier- und Einbandhaltbarkeit.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Profil climatique pour volumes reliés avec conditions optimisées pour la durabilité du papier et de la reliure.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Perfil climático para volúmenes encuadernados con condiciones optimizadas para la durabilidad del papel y la encuadernación.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: ملف مناخي للمجلدات الم_bindanvендах مع ظروف محسنة لمتانة الورق والتجليد.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Profil iklim untuk volume terikat dengan kondisi yang dioptimalkan untuk ketahanan kertas dan penjilidan.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 纸张和装订耐用性条件优化的装订卷册气候概况。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: BoekOpslagOmgeving

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: BuchLagerUmgebung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: EnvironnementStockageLivres

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: AmbienteAlmacenamientoLibros

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: بيئة تخزين الكتب

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: LingkunganPenyimpananBuku

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 图书存储环境

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Klimaatprofiel voor magnetische en optische media die stabiele en laag-verontreinigende opslagomstandigheden vereisen.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Klimaprofil für magnetische und optische Medien, die stabile und schadstoffarme Lagerbedingungen erfordern.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Profil climatique pour supports magnétiques et optiques nécessitant des conditions de stockage stables et peu contaminantes.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Perfil climático para medios magnéticos y ópticos que requieren condiciones de almacenamiento estables y de baja contaminación.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: ملف مناخي للوسائط المغناطيسية والبصرية التي تتطلب ظروف تخزين مستقرة ومنخفضة الملوثات.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Profil iklim untuk media magnetik dan optik yang memerlukan kondisi penyimpanan stabil dan rendah kontaminan.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 需要稳定和低污染存储条件的磁性和光学媒体气候概况。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: AudiovisueleOpslagOmgeving

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: AudiovisuelleLagerUmgebung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: EnvironnementStockageAudiovisuel

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: AmbienteAlmacenamientoAudiovisual

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: بيئة التخزين السمعي البصري

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: LingkunganPenyimpananAudiovisual

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 视听存储环境

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Klimaatprofiel voor digitale dragers met nadruk op stabiliteit en beperking van hardwarebehoudsrisico's.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Klimaprofil für digitale Datenträger mit Schwerpunkt auf Stabilität und Minderung von Hardwareerhaltungsrisiken.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Profil climatique pour supports numériques mettant l'accent sur la stabilité et l'atténuation des risques de préservation matérielle.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Perfil climático para soportes digitales que enfatiza la estabilidad y la mitigación de riesgos de preservación de hardware.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: ملف مناخي للحوامل الرقمية مع التركيز على الاستقرار والتخفيف من مخاطر الحفاظ على الأجهزة.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Profil iklim untuk media digital yang menekankan stabilitas dan mitigasi risiko pelestarian perangkat keras.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 强调稳定性和硬件保存风险缓解的数字载体气候概况。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: DigitaleMediaOpslagOmgeving

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: DigitaleMedienLagerUmgebung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: EnvironnementStockageMédiasNumériques

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: AmbienteAlmacenamientoMediosDigitales

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: بيئة تخزين الوسائط الرقمية

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: LingkunganPenyimpananMediaDigital

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 数字媒体存储环境

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Klimaatprofiel voor gemengde collecties waar basis milieucontrole voldoende is.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Klimaprofil für gemischte Bestände, bei dem grundlegende Umweltkontrolle ausreicht.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Profil climatique pour collections mixtes où un contrôle environnemental de base suffit.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Perfil climático para colecciones mixtas donde el control ambiental básico es suficiente.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: ملف مناخي للمجموعات المختلطة حيث يكون التحكم البيئي الأساسي كافيًا.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Profil iklim untuk koleksi campuran di mana kontrol lingkungan dasar sudah cukup.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 基本环境控制足够的混合馆藏气候概况。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: AlgemeneOpslagOmgeving

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: AllgemeineLagerUmgebung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: EnvironnementStockageGénéral

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: AmbienteAlmacenamientoGeneral

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: بيئة التخزين العامة

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: LingkunganPenyimpananUmum

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 通用存储环境

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Klimaatprofiel voor minimaal gecontroleerde opslagomstandigheden voornamelijk gebufferd door de gebouwschil.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Klimaprofil für minimal kontrollierte Lagerbedingungen, die hauptsächlich durch die Gebäudehülle gepuffert werden.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Profil climatique pour conditions de stockage minimalement contrôlées, tamponnées principalement par l'enveloppe du bâtiment.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Perfil climático para condiciones de almacenamiento mínimamente controladas amortiguadas principalmente por la envolvente del edificio.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: ملف مناخي لظروف التخزين ذات التحكم الأقل والتي يتم تخفيفها بشكل أساسي بواسطة غلاف المبنى.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Profil iklim untuk kondisi penyimpanan dengan kontrol minimal yang terutama dibuffer oleh selubung bangunan.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 主要由建筑围护结构缓冲的最低控制存储条件气候概况。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: OmgevingsOpslagOmgeving

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: UmgebungslagerUmgebung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: EnvironnementStockageAmbiant

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: AmbienteAlmacenamientoAmbiente

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: بيئة التخزين المحيطي

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: LingkunganPenyimpananAmbien

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 环境存储环境

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Klimaatprofiel voor geïsoleerde intake en behandelfasering vóór integratie met hoofdcollecties.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Klimaprofil für isolierte Aufnahme und Behandlungsstaging vor der Integration in Hauptbestände.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Profil climatique pour réception isolée et staging de traitement avant intégration aux collections principales.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Perfil climático para recepción aislada y staging de tratamiento antes de la integración con las colecciones principales.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: ملف مناخي للاستقبال المعزول وتجهيز المعالجة قبل الدمج مع المجموعات الرئيسية.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Profil iklim untuk penerimaan terisolasi dan staging pengobatan sebelum integrasi dengan koleksi utama.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 在与主馆藏整合之前进行隔离接收和处理分期的气候概况。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: QuarantaineOmgeving

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: QuarantäneUmgebung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: EnvironnementQuarantaine

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: AmbienteCuarentena

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: بيئة العزل

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: LingkunganKarantina

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 隔离环境

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Klimaatprofiel voor behandelingwerkruimten die gecontroleerde maar operationeel flexibele omstandigheden vereisen.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Klimaprofil für Behandlungsarbeitsbereiche, die kontrollierte aber betrieblich flexible Bedingungen erfordern.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Profil climatique pour espaces de traitement nécessitant des conditions contrôlées mais opérationnellement flexibles.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Perfil climático para espacios de tratamiento que requieren condiciones controladas pero operacionalmente flexibles.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: ملف مناخي لمساحات المعالجة التي تتطلب ظروفًا مضبوطة ولكن مرنة تشغيليًا.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Profil iklim untuk ruang kerja perawatan yang memerlukan kondisi terkontrol namun fleksibel secara operasional.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 需要受控但操作灵活条件的处理工作空间气候概况。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: ConserveringslabOmgeving

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: KonservierungslaborUmgebung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: EnvironnementLaboratoireConservation

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: AmbienteLaboratorioConservación

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: بيئة مختبر الحفظ

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: LingkunganLabKonservasi

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 保护实验室环境

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Klimaatprofiel voor gedocumenteerde configuraties buiten vooraf gedefinieerde categorieën.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Klimaprofil für dokumentierte Konfigurationen außerhalb vordefinierter Kategorien.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Profil climatique pour configurations documentées hors des catégories prédéfinies.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Perfil climático para configuraciones documentadas fuera de categorías predefinidas.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: ملف مناخي للتكوينات الموثقة خارج الفئات المحددة مسبقًا.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Profil iklim untuk konfigurasi terdokumentasi di luar kategori yang telah ditentukan sebelumnya.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 预定义类别之外的已记录配置的通用气候概况。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: OverigeMilieuZone

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: SonstigeUmgebungZone

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: AutreZoneEnvironnementale

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: OtraZonaAmbiental

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: منطقة بيئية أخرى

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: ZonaLingkunganLainnya

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 其他环境区域

        predicate: EXACT_SYNONYM

        in_language: zh
