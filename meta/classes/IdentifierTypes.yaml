id: https://nde.nl/ontology/hc/class/IdentifierTypes
name: IdentifierTypes
title: Identifier Types (Concrete Subclasses)
prefixes:
  rov: http://www.w3.org/ns/regorg#
  adms: http://www.w3.org/ns/adms#
  bibo: http://purl.org/ontology/bibo/
  linkml: https://w3id.org/linkml/
  hc: https://nde.nl/ontology/hc/
  skos: http://www.w3.org/2004/02/skos/core#
default_prefix: hc
imports:
  - ./IdentifierType
  - linkml:types
  - ../metadata
  - ../slots/has_code
  - ../slots/has_label
classes:
  ISILIdentifier:
    is_a: IdentifierType
    class_uri: hc:ISILIdentifier
    description: >-
      Unique alphanumeric code assigned to library and related organizations per ISO 15511.
    alt_descriptions:
      nl: Unieke alfanumerieke code toegewezen aan bibliotheken en gerelateerde organisaties volgens ISO 15511.
      de: Eindeutiger alphanumerischer Code, der Bibliotheken und verwandten Organisationen nach ISO 15511 zugewiesen wird.
      fr: Code alphanumérique unique attribué aux bibliothèques et organisations connexes selon ISO 15511.
      es: Código alfanumérico único asignado a bibliotecas y organizaciones relacionadas según ISO 15511.
      ar: رمز أبجدي رقمي فريد يُعين للمكتبات والمنظمات ذات الصلة وفقًا لـ ISO 15511.
      id: Kode alfanumerik unik yang ditetapkan untuk perpustakaan dan organisasi terkait sesuai ISO 15511.
      zh: 根据 ISO 15511 分配给图书馆及相关组织的唯一字母数字代码。
    structured_aliases:

      - literal_form: ISILIdentificatie

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: ISILKennung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: IdentifiantISIL

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: IdentificadorISIL

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف ISIL

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: IdentifikasiISIL

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: ISIL标识符

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Gezagscontrole-identificatie die linkt naar de VIAF-uniecatalogus.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Normdaten-ID, die mit dem Virtuellen Internationalen Normdatei-Verbundkatalog verknüpft.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Identifiant d'autorité liant au catalogue collectif du Fichier d'Autorité International Virtuel.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Identificador de control de autoridades que enlaza al catálogo colectivo del Archivo de Autoridades Virtual Internacional.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف سلطة يربط باتحاد الفهرس الدولي الافتراضي.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Pengidentifikasi kontrol otoritas yang menautkan ke katalog gabungan Virtual International Authority File.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 链接到虚拟国际规范文档联合目录的规范控制标识符。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: VIAFIdentificatie

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: VIAFKennung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: IdentifiantVIAF

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: IdentificadorVIAF

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف VIAF

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: IdentifikasiVIAF

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: VIAF标识符

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Persistente unieke identificatie voor bijdragers aan creatieve werken volgens ISO 27729.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Persistente eindeutige Kennung für Mitwirkende an kreativen Werken nach ISO 27729.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Identifiant unique persistant pour les contributeurs aux œuvres créatives selon ISO 27729.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Identificador único persistente para contribuyentes a obras creativas según ISO 27729.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف فريد دائم للمساهمين في الأعمال الإبداعية وفقًا لـ ISO 27729.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Pengidentifikasi unik persisten untuk kontributor karya kreatif sesuai ISO 27729.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 根据 ISO 27729 为创意作品贡献者提供的持久唯一标识符。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: ISNIIdentificatie

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: ISNIKennung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: IdentifiantISNI

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: IdentificadorISNI

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف ISNI

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: IdentifikasiISNI

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: ISNI标识符

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Open persistente identificatie voor onderzoeksorganisaties aangeboden door het Research Organization Registry.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Offener persistenter Identifikator für Forschungsorganisationen, bereitgestellt vom Research Organization Registry.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Identifiant persistant ouvert pour les organisations de recherche fourni par le Research Organization Registry.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Identificador persistente abierto para organizaciones de investigación proporcionado por el Research Organization Registry.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف دائم مفتوح للمنظمات البحثية يوفره سجل المنظمات البحثية.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Pengidentifikasi persisten terbuka untuk organisasi penelitian yang disediakan oleh Research Organization Registry.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 由研究组织注册表提供的研究组织开放持久标识符。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: RORIdentificatie

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: RORKennung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: IdentifiantROR

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: IdentificadorROR

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف ROR

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: IdentifikasiROR

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: ROR标识符

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Niet-eigendommelijke unieke identificatie voor onderzoekers en geleerden die toeschrijving over platforms mogelijk maakt.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Nicht-proprietärer eindeutiger Identifikator für Forscher und Wissenschaftler, der die Zuschreibung über Plattformen hinweg ermöglicht.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Identifiant unique non propriétaire pour les chercheurs et universitaires permettant l'attribution sur plusieurs plateformes.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Identificador único no propietario para investigadores y académicos que permite la atribución entre plataformas.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف فريد غير مملوك للباحثين والعلماء يمكّن من النسب عبر المنصات.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Pengidentifikasi unik non-proprietari untuk peneliti dan cendekiawan yang memungkinkan atribusi lintas platform.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 研究人员和学者的非专有唯一标识符，支持跨平台归属。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: ORCIDIdentificatie

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: ORCIDKennung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: IdentifiantORCID

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: IdentificadorORCID

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف ORCID

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: IdentifikasiORCID

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: ORCID标识符

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Unieke entiteitsidentificatie binnen de Wikidata-kennisgraaf met Q-nummerformaat.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Eindeutiger Entitätsidentifikator innerhalb des Wikidata-Wissensgrafen im Q-Nummer-Format.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Identifiant d'entité unique dans le graphe de connaissances Wikidata utilisant le format Q-numéro.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Identificador de entidad único dentro del grafo de conocimiento de Wikidata usando formato de número Q.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف كيان فريد داخل الرسم البياني للمعرفة ويكي بيانات باستخدام تنسيق رقم Q.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Pengidentifikasi entitas unik dalam graf pengetahuan Wikidata menggunakan format nomor Q.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 维基数据知识图谱中使用 Q 编号格式的唯一实体标识符。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: WikidataIdentificatie

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: WikidataKennung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: IdentifiantWikidata

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: IdentificadorWikidata

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف ويكي بيانات

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: IdentifikasiWikidata

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 维基数据标识符

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Gezagsrecordidentificatie uit de Duitse Gemeinsame Normdatei.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Normdatensatz-ID aus der Gemeinsamen Normdatei.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Identifiant d'autorité du fichier d'autorité intégré allemand.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Identificador de registro de autoridad del Archivo de Autoridad Integrado Alemán.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف سجل سلطة من ملف السلطة الألماني الموحد.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Pengidentifikasi catatan otoritas dari Berkas Otoritas Terpadu Jerman.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 来自德国综合规范文档的规范记录标识符。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: GNDIdentificatie

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: GNDKennung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: IdentifiantGND

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: IdentificadorGND

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف GND

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: IdentifikasiGND

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: GND标识符

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Gezagsidentificatie uit de Getty Union List of Artist Names vocabulaire.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Normdaten-ID aus dem Getty Union List of Artist Names Vokabular.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Identifiant d'autorité du vocabulaire Union List of Artist Names du Getty.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Identificador de autoridad del vocabulario Lista Unión de Nombres de Artistas del Getty.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف سلطة من مفردات قائمة اتحاد أسماء الفنانين غيتي.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Pengidentifikasi otoritas dari kosakata Union List of Artist Names Getty.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 来自盖蒂艺术家名称联合列表词汇表的规范标识符。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: ULANIdentificatie

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: ULANKennung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: IdentifiantULAN

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: IdentificadorULAN

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف ULAN

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: IdentifikasiULAN

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: ULAN标识符

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Conceptidentificatie uit de Getty Art and Architecture Thesaurus vocabulaire.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Konzept-ID aus dem Getty Art and Architecture Thesaurus Vokabular.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Identifiant de concept du vocabulaire Art and Architecture Thesaurus du Getty.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Identificador de concepto del vocabulario Tesauro de Arte y Arquitectura del Getty.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف مفهوم من مفردات فن وفن العمارة غيتي.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Pengidentifikasi konsep dari kosakata Art and Architecture Thesaurus Getty.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 来自盖蒂艺术与建筑叙词表词汇表的概念标识符。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: AATIdentificatie

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: AATKennung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: IdentifiantAAT

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: IdentificadorAAT

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف AAT

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: IdentifikasiAAT

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: AAT标识符

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Plaatsidentificatie uit de Getty Thesaurus of Geographic Names vocabulaire.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Orts-ID aus dem Getty Thesaurus of Geographic Names Vokabular.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Identifiant de lieu du vocabulaire Thesaurus of Geographic Names du Getty.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Identificador de lugar del vocabulario Tesauro de Nombres Geográficos del Getty.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف مكان من مفردات تيسوروس الأسماء الجغرافية غيتي.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Pengidentifikasi tempat dari kosakata Thesaurus of Geographic Names Getty.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 来自盖蒂地理名称叙词表词汇表的地点标识符。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: TGNIdentificatie

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: TGNKennung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: IdentifiantTGN

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: IdentificadorTGN

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف TGN

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: IdentifikasiTGN

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: TGN标识符

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Registratienummer toegewezen aan Nederlandse handelsentiteiten door de Kamer van Koophandel.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Registriernummer, die niederländischen Handelsgesellschaften von der Handelskammer zugewiesen wird.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Numéro d'enregistrement attribué aux entités commerciales néerlandaises par la Chambre de Commerce.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Número de registro asignado a entidades comerciales neerlandesas por la Cámara de Comercio.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: رقم تسجيل يُعين للكيانات التجارية الهولندية من قبل غرفة التجارة.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Nomor registrasi yang ditetapkan untuk entitas komersial Belanda oleh Kamar Dagang.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 由商会分配给荷兰商业实体的注册号。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: KvKIdentificatie

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: KvKKennung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: IdentifiantKvK

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: IdentificadorKvK

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف KvK

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: IdentifikasiKvK

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: KvK标识符

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Gestandaardiseerde unieke identificatie voor rechtspersonen die deelnemen aan financiële transacties volgens ISO 17442.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Standardisierte eindeutige Kennung für juristische Personen, die an Finanztransaktionen teilnehmen, nach ISO 17442.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Identifiant unique normalisé pour les entités légales participant à des transactions financières selon ISO 17442.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Identificador único estandarizado para entidades legales que participan en transacciones financieras según ISO 17442.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف فريد موحد للكيانات القانونية المشاركة في المعاملات المالية وفقًا لـ ISO 17442.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Pengidentifikasi unik terstandarisasi untuk entitas legal yang berpartisipasi dalam transaksi keuangan sesuai ISO 17442.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 根据 ISO 17442 为参与金融交易的法律实体提供的标准化唯一标识符。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: LEIIdentificatie

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: LEIKennung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: IdentifiantLEI

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: IdentificadorLEI

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف LEI

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: IdentifikasiLEI

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: LEI标识符

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Belastingregistratienummer voor entiteiten die btw-plichtig zijn in rechtsgebieden.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Steuerregisternummer für entitäten, die in Rechtsgebieten mehrwertsteuerpflichtig sind.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Numéro d'enregistrement fiscal pour les entités assujetties à la taxe sur la valeur ajoutée dans les juridictions.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Número de registro fiscal para entidades sujetas al impuesto al valor agregado en jurisdicciones.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: رقم التسجيل الضريبي للكيانات الخاضعة لضريبة القيمة المضافة في الولايات القضائية.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Nomor registrasi pajak untuk entitas yang terkena pajak pertambahan nilai di yurisdiksi.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 在司法管辖区内缴纳增值税的实体的税务登记号。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: BTWIdentificatie

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: UmsatzsteuerKennung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: IdentifiantTVA

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: IdentificadorIVA

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف ضريبة القيمة المضافة

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: IdentifikasiPPN

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 增值税标识符

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Uniek numeriek commercieel boekidentificatienummer dat voldoet aan de ISO 2108-standaard.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Eindeutige numerische kommerzielle Buchkennung nach ISO 2108-Norm.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Identifiant numérique unique de livre commercial conforme à la norme ISO 2108.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Identificador numérico único de libro comercial que cumple con la norma ISO 2108.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف رقمي فريد للكتاب التجاري يتوافق مع معيار ISO 2108.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Pengidentifikasi numerik unik buku komersial yang mematuhi standar ISO 2108.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 符合 ISO 2108 标准的唯一数字商业图书标识符。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: ISBNIdentificatie

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: ISBNummer

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: IdentifiantISBN

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: IdentificadorISBN

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف ISBN

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: IdentifikasiISBN

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: ISBN标识符

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Uniek numeriek identificatienummer voor periodieke publicaties volgens ISO 3297-standaard.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Eindeutige numerische Kennung für fortlaufende Sammelwerke nach ISO 3297-Norm.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Identifiant numérique unique pour les publications en série conforme à la norme ISO 3297.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Identificador numérico único para publicaciones seriadas que cumple con la norma ISO 3297.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف رقمي فريد للمنشورات المسلسلة يتوافق مع معيار ISO 3297.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Pengidentifikasi numerik unik untuk publikasi berseri yang mematuhi standar ISO 3297.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 符合 ISO 3297 标准的连续出版物唯一数字标识符。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: ISSNIdentificatie

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: ISSNKennung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: IdentifiantISSN

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: IdentificadorISSN

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف ISSN

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: IdentifikasiISSN

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: ISSN标识符

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Persistente alfanumerieke identificatie voor digitale objecten volgens ISO 26324-standaard.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Persistenter alphanumerischer Identifikator für digitale Objekte nach ISO 26324-Norm.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Identifiant alphanumérique persistant pour les objets numériques conforme à la norme ISO 26324.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Identificador alfanumérico persistente para objetos digitales que cumple con la norma ISO 26324.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف أبجدي رقمي دائم للكائنات الرقمية يتوافق مع معيار ISO 26324.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Pengidentifikasi alfanumerik persisten untuk objek digital yang mematuhi standar ISO 26324.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 符合 ISO 26324 标准的数字对象持久字母数字标识符。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: DOIIdentificatie

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: DOIKennung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: IdentifiantDOI

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: IdentificadorDOI

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف DOI

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: IdentifikasiDOI

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: DOI标识符

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Uniek recordnummer binnen de OCLC WorldCat bibliografische database.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Eindeutige Datensatznummer innerhalb der OCLC WorldCat bibliografischen Datenbank.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Numéro d'enregistrement unique dans la base de données bibliographique WorldCat de l'OCLC.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Número de registro único dentro de la base de datos bibliográfica WorldCat de OCLC.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: رقم سجل فريد داخل قاعدة البيانات الببليوغرافية WorldCat OCLC.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Nomor rekaman unik dalam database bibliografi WorldCat OCLC.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: OCLC WorldCat 书目数据库中的唯一记录号。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: OCLCIdentificatie

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: OCLCKennung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: IdentifiantOCLC

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: IdentificadorOCLC

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف OCLC

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: IdentifikasiOCLC

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: OCLC标识符

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Referentienummer binnen een uitgebreide wetenschappelijke catalogus die het volledige oeuvre van een kunstenaar documenteert.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Referenznummer innerhalb eines umfassenden wissenschaftlichen Katalogs, der das vollständige Werk eines Künstlers dokumentiert.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Numéro de référence dans un catalogue savant complet documentant l'œuvre complet d'un artiste.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Número de referencia dentro de un catálogo académico integral que documenta la obra completa de un artista.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: رقم مرجعي داخل فهرس علمي شامل يوثق الأعمال الكاملة لفنان.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Nomor referensi dalam katalog ilmiah komprehensif yang mendokumentasikan karya lengkap seorang seniman.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 在记录艺术家完整作品的综合学术目录中的参考编号。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: CatalogusRaisonnéNummer

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: CatalogueRaisonnéNummer

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: NuméroCatalogueRaisonné

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: NúmeroCatálogoRazonado

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: رقم الفهرس الم raisoné

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: NomorCatalogRaisonne

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 作品目录编号

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Intern volgnummer toegekend door een collectie-beherende instelling.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Interne Verfolgungsnummer, die von einer sammelnden Institution vergeben wird.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Numéro de suivi interne attribué par une institution détentrice de collection.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Número de seguimiento interno asignado por una institución poseedora de colección.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: رقم تتبع داخلي يُعين من قبل مؤسسة حائزة لمجموعة.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Nomor pelacakan internal yang ditetapkan oleh institusi pemegang koleksi.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 由馆藏机构分配的内部追踪编号。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Inventarisnummer

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Inventarnummer

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: NuméroInventaire

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: NúmeroInventario

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: رقم الجرد

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: NomorInventaris

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 库存编号

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Formeel nummer toegekend bij de verwerving van een object in een collectie.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Formale Nummer, die bei der Erwerbung eines Objekts in eine Sammlung vergeben wird.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Numéro formel attribué lors de l'acquisition d'un objet dans une collection.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Número formal asignado al adquirir un objeto para una colección.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: رقم رسمي يُعند عند اقتناء شيء لمجموعة.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Nomor formal yang ditetapkan saat perolehan objek ke dalam koleksi.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 在将物品收入馆藏时分配的正式编号。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Toegangsnummer

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Zugangsnummer

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: NuméroAccession

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: NúmeroAdquisición

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: رقم الانضمام

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: NomorAksesi

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 入藏编号

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Unieke referentie voor archiefinventarissen die collectie-inhoud beschrijven.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Eindeutige Referenz für archivische Findhilfen, die Sammlungsinhalte beschreiben.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Référence unique pour les instruments de recherche archivistiques décrivant le contenu des collections.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Referencia única para guías de búsqueda archivísticas que describen el contenido de la colección.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: مرجع فريد لأدلة البحث الأرشيفية التي تصف محتويات المجموعة.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Referensi unik untuk panduan pencarian arsip yang menjelaskan konten koleksi.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 描述馆藏内容的档案检索工具的唯一引用。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: InventarisIdentificatie

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: FindhilfeKennung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: IdentifiantInstrumentRecherche

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: IdentificadorGuíaBúsqueda

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف دليل البحث

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: IdentifikasiPanduanPencarian

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 检索工具标识符

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Identificatie voor een archieffonds als bovenliggende aggregatie van archiefbescheiden.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Kennung für einen Archivfond als übergeordnete Aggregation von Archivgut.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Identifiant pour un fonds d'archives comme agrégation de niveau supérieur de documents.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Identificador para un fondo de archivo como agregación de nivel superior de documentos.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف لأرشيف كمجموعة مستوية عليا من الوثائق.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Pengidentifikasi untuk fond arsip sebagai agregasi tingkat atas catatan.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 作为记录顶级聚合的档案全宗的标识符。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Fondsnummer

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Fondsnummer

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: NuméroFonds

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: NúmeroFondo

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: رقم الأرشيف

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: NomorFonds

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 全宗号

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Identificatie voor documenten gecodeerd in het Encoded Archival Description XML-formaat.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Kennung für Dokumente im Encoded Archival Description XML-Format kodiert.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Identifiant pour les documents encodés au format XML Encoded Archival Description.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Identificador para documentos codificados en formato XML Encoded Archival Description.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف للمستندات المشفرة بتنسيق Encoded Archival Description XML.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Pengidentifikasi untuk dokumen yang dikodekan dalam format XML Encoded Archival Description.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 以编码档案描述 XML 格式编码的文档的标识符。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: EADIdentificatie

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: EADKennung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: IdentifiantEAD

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: IdentificadorEAD

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف EAD

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: IdentifikasiEAD

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: EAD标识符

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Instellingsspecifiek identificatieschema voor intern gedefinieerde collecties.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Institutionsspezifisches Identifikationsschema für intern definierte Sammlungen.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Schéma d'identifiant spécifique à l'institution pour les collections définies en interne.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Esquema de identificador específico de la institución para colecciones definidas internamente.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: نظام معرف خاص بالمؤسسة للمجموعات المحددة داخليًا.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Skema pengidentifikasi khusus institusi untuk koleksi yang ditentukan secara internal.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 机构特定的内部定义馆藏标识符方案。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: LokaleCollectieIdentificatie

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: LokaleSammlungskennung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: IdentifiantCollectionLocale

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: IdentificadorColecciónLocal

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف المجموعة المحلية

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: IdentifikasiKoleksiLokal

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 本地馆藏标识符

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Niet-openbare referentiecode gebruikt binnen de interne systemen van een instelling.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Nicht-öffentlicher Referenzcode, der in den internen Systemen einer Institution verwendet wird.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Code de référence non public utilisé dans les systèmes internes d'une institution.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Código de referencia no público utilizado dentro de los sistemas internos de una institución.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: رمز مرجعي غير عام مستخدم ضمن الأنظمة الداخلية لمؤسسة.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Kode referensi non-publik yang digunakan dalam sistem internal suatu institusi.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 在机构内部系统中使用的非公开参考代码。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: InterneCodeIdentificatie

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: InternerCodeKennung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: IdentifiantCodeInterne

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: IdentificadorCodigoInterno

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف الرمز الداخلي

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: IdentifikasiKodeInternal

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 内部代码标识符

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Uniforme reeks tekens die een abstracte of fysieke bron identificeert.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Gleichförmige Zeichenfolge, die eine abstrakte oder physische Ressource identifiziert.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Séquence uniforme de caractères identifiant une ressource abstraite ou physique.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Secuencia uniforme de caracteres que identifica un recurso abstracto o físico.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: تسلسل موحد من الأحرف يحدد موردًا مجردًا أو ماديًا.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Urutan karakter seragam yang mengidentifikasi sumber daya abstrak atau fisik.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 标识抽象或物理资源的统一字符序列。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: URIIdentificatie

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: URKennung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: IdentifiantURI

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: IdentificadorURI

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف URI

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: IdentifikasiURI

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: URI标识符

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: Webadres dat een bronlocatie specificeert die toegankelijk is via HTTP-protocollen.

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Webadresse, die einen Ressourcenstandort angibt, der über HTTP-Protokolle zugänglich ist.

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Adresse web spécifiant un emplacement de ressource accessible via les protocoles HTTP.

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: Dirección web que especifica una ubicación de recurso accesible a través de protocolos HTTP.

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: عنوان ويب يحدد موقع مورد يمكن الوصول إليه عبر بروتوكولات HTTP.

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: Alamat web yang menentukan lokasi sumber daya yang dapat diakses melalui protokol HTTP.

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 指定可通过 HTTP 协议访问的资源位置的网址。

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: URLIdentificatie

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: URLKennung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: IdentifiantURL

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: IdentificadorURL

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: معرف URL

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: IdentifikasiURL

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: URL标识符

        predicate: EXACT_SYNONYM

        in_language: zh
