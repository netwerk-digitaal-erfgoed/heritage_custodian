id: https://nde.nl/ontology/hc/class/CustodianObservation
name: CustodianObservation
title: Custodian Observation Class
prefixes:
  linkml: https://w3id.org/linkml/
  hc: https://nde.nl/ontology/hc/
  pico: https://personsincontext.org/model#
  prov: http://www.w3.org/ns/prov#
  schema: http://schema.org/
  crm: http://www.cidoc-crm.org/cidoc-crm/
  rico: https://www.ica.org/standards/RiC/ontology#
default_prefix: hc
imports:
  - linkml:types
  - ../slots/has_label
  - ../slots/has_type
  - ../slots/has_value
  - ../slots/has_context
  - ../slots/temporal_extent
  - ../slots/has_source
  - ../slots/observe
  - ../slots/refer_to
  - ../slots/has_score
classes:
  CustodianObservation:
    class_uri: hc:CustodianObservation
    description: >-
      Source-attributed evidence record serving as reconstruction input for establishing custodian attributes.
    alt_descriptions:
      nl: >-
        Bron-toegeschreven bewijsrecord dat dient als reconstructie-input voor het vaststellen van beheerdersattributen.
      de: >-
        Quellenzugeschriebener Nachweis, der als Rekonstruktionseingabe zur Ermittlung von Verwalterattributen dient.
      fr: >-
        Registre de preuve attribué à la source servant d'entrée de reconstruction pour établir les attributs du dépositaire.
      es: >-
        Registro de prueba atribuido a la fuente que sirve como entrada de reconstrucción para establecer atributos del custodio.
      ar: >-
        سجل دليل منسوب للمصدر يعمل كمدخل إعادة بناء لتأسيس سمات الأمين.
      id: >-
        Rekaman bukti yang diatribusikan ke sumber yang berfungsi sebagai input rekonstruksi untuk menetapkan atribut kustodian.
      zh: >-
        作为重建输入用于确立保管人属性的来源归属证据记录。
    structured_aliases:

      - literal_form: beheerderobservatie

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: bronwaarneming

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: Verwalterbeobachtung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: Quellenbeobachtung

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: observation du dépositaire

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: observation source

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: observación del custodio

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: observación fuente

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: ملاحظة الأمين

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: ملاحظة المصدر

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: observasi kustodian

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: observasi sumber

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 保管人观察

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: 来源观察

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: custodian observation

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: source evidence

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: reconstruction input

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: attributed data

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: evidence record

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: prov:Entity

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: pico:PersonObservation

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: crm:E73_Information_Object

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: rico:Record

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_label

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_value

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_type

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: refer_to

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_context

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: temporal_extent

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_source

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: observe

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_score

        predicate: EXACT_SYNONYM

        in_language: zh
