id: https://nde.nl/ontology/hc/classes/NormalizedLocation
name: NormalizedLocation
title: NormalizedLocation
prefixes:
  linkml: https://w3id.org/linkml/
  hc: https://nde.nl/ontology/hc/
  schema: http://schema.org/
  prov: http://www.w3.org/ns/prov#
  xsd: http://www.w3.org/2001/XMLSchema#
  locn: http://www.w3.org/ns/locn#
  geo: http://www.w3.org/2003/01/geo/wgs84_pos#
imports:
- linkml:types
# default_range: string
classes:
  NormalizedLocation:
    description: >-
      Standardized geographic reference with parsed address elements and optional geospatial coordinates.
    alt_descriptions:
      nl: >-
        Gestandaardiseerde geografische verwijzing met ontlede adresselementen en optionele geospatiale coördinaten.
      de: >-
        Standardisierte geografische Referenz mit analysierten Adresselementen und optionalen geospatiale Koordinaten.
      fr: >-
        Référence géographique normalisée avec éléments d'adresse analysés et coordonnées géospatiales optionnelles.
      es: >-
        Referencia geográfica estandarizada con elementos de dirección analizados y coordenadas geoespaciales opcionales.
      ar: >-
        مرجع جغرافي معياري مع عناصر عنوان محللة وإحداثيات جغرافية مكانية اختيارية.
      id: >-
        Referensi geografis terstandarisasi dengan elemen alamat yang diurai dan koordinat geospasial opsional.
      zh: >-
        具有解析地址元素和可选地理空间坐标的标准化地理参考。
    structured_aliases:

      - literal_form: genormaliseerde locatie

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: gestandaardiseerd adres

        predicate: EXACT_SYNONYM

        in_language: nl

      - literal_form: normalisierter Standort

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: standardisierte Adresse

        predicate: EXACT_SYNONYM

        in_language: de

      - literal_form: localité normalisée

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: adresse normalisée

        predicate: EXACT_SYNONYM

        in_language: fr

      - literal_form: ubicación normalizada

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: dirección normalizada

        predicate: EXACT_SYNONYM

        in_language: es

      - literal_form: الموقع المعياري

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: العنوان الموحد

        predicate: EXACT_SYNONYM

        in_language: ar

      - literal_form: lokasi ternormalisasi

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: alamat terstandarisasi

        predicate: EXACT_SYNONYM

        in_language: id

      - literal_form: 规范化位置

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: 标准化地址

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: address normalization

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: geocoding

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: location standardization

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: postal address

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: geographic coordinates

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: schema:Place

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: locn:Address

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: geo:SpatialThing

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_latitude

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_longitude

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_geographic_subdivision

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_code

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: in_country

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_postal_code

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_coordinates

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: has_note

        predicate: EXACT_SYNONYM

        in_language: zh

      - literal_form: 'Preserved from prior description: Normalized location representation with standardized address components and optional coordinates.'

        predicate: EXACT_SYNONYM

        in_language: zh
