id: https://nde.nl/ontology/hc/enum/DetectionLevelEnum
name: detection_level_enum
title: Detection Level Enum
prefixes:
  linkml: https://w3id.org/linkml/
  hc: https://nde.nl/ontology/hc/
default_prefix: hc
imports:
  - linkml:types
  - ../metadata

enums:
  DetectionLevelEnum:
    description: Granularity of scene/shot detection
    permissible_values:
      SHOT:
        description: Technical camera edit boundaries
      SCENE:
        description: Semantic content/location boundaries
      BOTH:
        description: Both shot and scene detection
